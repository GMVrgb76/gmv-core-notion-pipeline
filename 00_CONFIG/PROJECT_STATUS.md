# GMV Project Status — Operational Ledger

## Operational Rules

1. `PROJECT_STATUS.md` is the single operational ledger for this project.
2. Every completed task updates `PROJECT_STATUS.md`.
3. No task is considered complete until `PROJECT_STATUS.md` has been updated.
4. `PROJECT_STATUS.md` records facts only. No speculation. No architectural
   opinions. No implementation proposals.
5. Historical records remain append-only. Corrections are added as new
   entries. Previous history is never silently rewritten.
6. Every future task, without exception, must end by updating
   `PROJECT_STATUS.md`, appending the fixed "PROJECT STATUS UPDATE" block
   defined in the Task Completion Protocol below.
7. `PROJECT_STATUS.md` is the unique operational source of truth for
   current project state. ADRs remain the authoritative architectural
   decision records; freeze documents remain the authoritative component
   lifecycle records. This document indexes and summarizes them — it never
   replaces or restates their content.
8. Historical reports (archaeology reports, task outputs) are immutable
   once completed and are never edited after completion. Corrections take
   the form of new entries, per Rule 5.
9. Every future task must verify that `PROJECT_STATUS.md` has been updated
   before declaring the task complete.

This document is not an ADR, not an archaeology report, and not a handoff.
It is a living ledger. ADRs, freeze records, and archaeology reports remain
the source of full reasoning; this document only tracks current state,
active decisions, and the chronological record of what happened.

---

## Task Completion Protocol

Every future task must end its own output with a fixed "PROJECT STATUS
UPDATE" block, and must reflect that same information here (§1 Dashboard,
and §4 Timeline for the completed entry) before the task is declared
complete.

Required block, verbatim field set:

```
## PROJECT STATUS UPDATE

- Current Phase: <...>
- Current Approved Task: <...>
- Tasks Completed: <...>
- Architecture Changed (YES/NO): <...>
- ADR Created: <...>
- Freeze Created: <...>
- Documents Updated: <...>
- Next Approved Task: <...>
- Known Risks: <...>
- Working Tree Status: <...>
- Commit: <...>
```

A task is not complete until this block has been produced in the task's
own report and `PROJECT_STATUS.md` has been updated to match it.

---

## 1. Project Dashboard

*Current state only. For history, see §4. For architectural reasoning, see §2 and the referenced documents.*

- **Current Phase:** GMV Core Development. Stabilization is formally closed (`00_CONFIG/STABILIZATION_CLOSED.md`).
- **Current Objective:** No GMV Core Development task has yet been opened. Deferred items from Stabilization (`PROJECT_STATUS.md` §3, `STABILIZATION_CLOSED.md` §6) are the known candidate backlog, none of them blocking.
- **Last Completed Task:** GMV V2 Sprint 003 (Data/Database) — `MAIN-011` governed-freeze declaration (documentation only, no code). An exhaustive search of `~/.gmv_core` (tracked, untracked, gitignored) and every `com.gmv.*` LaunchAgent's target script found **no writer, present or authorized**, to `03_STATE/objects/*.json` or `02_INDEXES/OBJECT_INDEX.json`. `~/GMV_CORE` and any other root external to `~/.gmv_core` were explicitly not part of this verification. Recorded as an addendum to `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`: these two artifacts are **runtime-derived, non-authoritative state** (SQLite `PER-000001` remains sole canonical source); no writer to them is authorized; any future writer requires explicit Project Owner approval and must go through `gmv_core.identity`/`gmv_core.repositories.identity`, never hand-written JSON. **Three distinct concepts are now explicitly separated and must not be conflated:** (1) **governed freeze** — this declarative, policy-level statement, enforced by no code; (2) **diagnostic enforcement** — the `identity.json_conformance` check added in the prior slice, which detects and reports a conflict only *after* a write has already occurred; (3) **filesystem immutability — does not exist.** No permission change, ACL, lock, or other technical control prevents a write to either artifact today. This task does **not** resolve, close, or alter `ARC-005`, `DB-020`, or `ARC-009` — their circular dependencies on `MAIN-011` are unchanged. GMV Core Development itself remains not yet opened as a distinct phase in this ledger; Sprint 003 is tracked as its own GMV V2 program item (see Timeline entry below).
- **Next Approved Task:** None formally approved. `MAIN-011`'s remaining scope — physical prevention of new parallel JSON writes (distinct from both the governed-freeze declaration and the diagnostic check, neither of which is a technical control), and the broader multi-store reconciliation under `ARC-005` — requires its own future authorization and remains blocked on the `ARC-005`↔`MAIN-011` and `DB-020`↔`ARC-009` circular backlog dependencies, unchanged by this task. GMV Core Development (this ledger's own phase) has not been opened by any specific task.
- **Last Commit:** this task's commit (see `git log`) — "docs: declare MAIN-011 governed freeze (no writer present or authorized)".
- **Repository Status:** Clean — no unexplained tracked modifications as of the last System Authority Audit (Post-REBASE 001 Task 3, 2026-07-13). Untracked material remains present (see §7, Automation Review) but is inventoried, not unexplained.
- **Runtime Status:** One native Core service (`SRV-000001` Knowledge Engine) and three compatibility services (`SRV-000002` Morning Brief, `SRV-000003` Daily Log, `SRV-000004` Market Engine) are registered and active. Four components are formally frozen (Apprentice, Constitution CLI feature, Real Estate orchestration, GMV Engine/Decision Engine automation). Fenix, `com.gmv.dailybrief`, and `com.gmv.morningbrief.email` are provisionally suspended pending technical review (operational suspension only — none classified as legacy/frozen/rejected). No live, ungoverned, scheduled automation remains loaded. A scheduled backup automation (`com.gmv.backup`, 15-minute interval) runs a governed, verified full-system backup with a hash-chained audit trail; isolated restore verification is a human-owned manual operation (`gmv snapshot restore-check`), not scheduled.
- **Current Risks:** See §6, Open Questions and §7, Project Health.
- **Current Blockers:** None.
- **Development Readiness:** Ready — Stabilization is closed; GMV Core Development may begin once a task is explicitly opened.
- **Last Updated:** 2026-07-18.

---

## 2. Permanent Architectural Decisions

*Only active decisions. Full reasoning lives in the referenced ADRs/freeze records — never duplicated here.*

| Category | Decision | Reference |
|---|---|---|
| **Repository** | Dropbox (`GMV_MASTER_SYSTEM`) is the canonical Repository for non-runtime material; it is never the canonical source for executable runtime code. | `00_CONFIG/REBASE_001_CLOSED.md` §"Major Architectural Decisions" |
| **Runtime** | Runtime code belongs to GMV Core (`~/.gmv_core`, git-tracked). | `00_CONFIG/REBASE_001_CLOSED.md` §"Major Architectural Decisions" |
| **Compatibility** | Legacy engines execute through a hash-pinned Compatibility Layer (`10_API/gmv_compatibility.py`) with fail-closed release verification. Registered services: `SRV-000002` Morning Brief, `SRV-000003` Daily Log, `SRV-000004` Market Engine. | `00_CONFIG/SERVICE_SPECIFICATION.md`, `00_CONFIG/LEGACY_ENGINE_INVENTORY.md` |
| **Legacy** | Frozen, preserved, not deleted: Apprentice; Constitution CLI feature; Real Estate orchestration (`realestate_runner.py` family); Dropbox `market_engine.py` (reclassified `HISTORICAL_BEHAVIOUR_ONLY`); GMV Engine/Decision Engine automation. | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`, `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`, `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`, `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`, `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md` |
| **Experimental** | A Local Coding Engine (v0, Draft status) exists as a separate, explicitly-scoped, read-only-first initiative. | `00_CONFIG/LOCAL_CODING_ENGINE.md` |
| **Primary Engineering** | Knowledge Engine (`SRV-000001`, `01_RUNTIME/knowledge_engine.py`) is the native Core runtime and the successor to the frozen Apprentice concept. | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` |
| **Independent Validation** | Divergent implementations are validated via isolated, `mktemp`-based fixture execution — never against production data — before any output-contract decision is made. | Post-REBASE 001 Task 2 (methodology; report only, no standalone document) |
| **Core Package** | `gmv_core/` (created Sprint 001) is the designated target package for `ARC-001`/`MAIN-001` ("executable Core"). Migration onto it is incremental, one named service per task. `ARC-002`'s static enforcement check (rejecting direct `sqlite3.connect` outside `gmv_core`) is deferred until `DB-002`/`DB-003` (foreign keys, domain constraints) land — it blocks final enforcement, not initial construction. | `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md` |

---

## 3. Active Work

*Current work queue. Status reflects actual authorization state, not aspiration.*

| Item | Status | Owner | Priority | Dependencies |
|---|---|---|---|---|
| Fenix final disposition (`com.gmv.fenix` / `gmv_fenix_engine.sh`) | PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW (Post-REBASE 001 Task 7; see `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md` for minimum requirements) | Unassigned | Medium (operational exposure removed; final disposition not urgent) | None |
| `com.gmv.dailybrief` / `com.gmv.morningbrief.email` final disposition | PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW (this task; see `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md` for minimum requirements) | Unassigned | Medium (operational exposure removed; final disposition not urgent) | None |
| `~/GMV_CORE` root — broader classification beyond its two (now suspended) live LaunchAgents | IDENTIFIED, unscheduled; root verdict `PRESERVE_PENDING_REVIEW`; ~40 further scripts and the `GMV_REDUCED` subsystem not yet audited | Unassigned | Low | None |
| Classify Dropbox `property_engine.py` | IDENTIFIED, unscheduled (open since REBASE 001 Task 3) | Unassigned | Medium | None |
| Archaeology of `gmv_watchdog.sh` and `gmv_orchestrator.sh` | IDENTIFIED, unscheduled (flagged by System Authority Audit and GMV Engine/Fenix archaeology as sharing the same governance gap) | Unassigned | Medium | None |
| Diagnose persistent `gmv status` CLI failure under `launchd` | IDENTIFIED, unscheduled (evidence gap in Post-REBASE 001 Tasks 4 and 6) | Unassigned | Medium | None |

---

## 4. Timeline

*Append-only. Never rewritten. Corrections are added as new entries below, not edits to existing ones.*

| Date | Task | Decision | Documents | Commit | Next task |
|---|---|---|---|---|---|
| 2026-07-12 | REBASE 001 Tasks 1–4 (Core migration scope; Dropbox executable classification; duplicate/divergent resolution; Apprentice authority formalization, revised) | Governing principles established: Runtime belongs to Core, Dropbox is Repository | Report-only | none | Migrate Apprentice runtime |
| 2026-07-12 | REBASE 001 Task 5 — Migrate Apprentice runtime into git-tracked Core | Complete | `01_RUNTIME/apprentice/apprentice_runtime.py` | `90d1942` | Apprentice complete archaeology |
| 2026-07-12 | Apprentice Complete Archaeology and Conditional Patching (unnumbered) | `FREEZE_AS_LEGACY` | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` | `113cd1f` | Update freeze doc with launchd disable detail |
| 2026-07-12 | Apprentice freeze record update (launchd persistent-disable documentation) | `FREEZE_AS_LEGACY` (unchanged, documented more fully) | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` | `fd1b851` | doctor_service.py archaeology |
| 2026-07-12 | REBASE 001 Tasks 6–7 — Forensic archaeology and revert of `doctor_service.py` | `SAFE_TO_REVERT`; reverted to `HEAD` | Report-only; no commit (file returned byte-identical to `HEAD`) | none | `11_CLI/gmv` archaeology |
| 2026-07-12 | REBASE 001 Tasks 8–9 — Forensic archaeology and revert of `11_CLI/gmv` (Constitution CLI feature) | `FREEZE_UNAPPROVED_FEATURE`; CLI wiring reverted to `HEAD` | Report-only; no commit (file returned byte-identical to `HEAD`) | none | Document Constitution CLI freeze |
| 2026-07-12 | REBASE 001 Task 10 — Document Constitution CLI feature freeze | Complete | `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md` | `e8e9459` | Interim architectural report |
| 2026-07-12 | REBASE 001 Task 11 — Interim architectural report | Complete | `00_CONFIG/REBASE_001_INTERIM_STATE.md` | `a2102a5` | `realestate_runner.py` archaeology |
| 2026-07-12 | REBASE 001 Task 12 — Forensic archaeology of `realestate_runner.py` | `FREEZE_AS_LEGACY` | Report-only | none | Document Real Estate legacy freeze |
| 2026-07-12 | REBASE 001 Task 13 — Document Real Estate legacy freeze | Complete | `00_CONFIG/REALESTATE_LEGACY_FREEZE.md` | `ac17e9b` | Close REBASE 001 |
| 2026-07-12 | REBASE 001 Closure | Complete | `00_CONFIG/REBASE_001_CLOSED.md`, `00_CONFIG/WHAT_TO_DO_NEXT.md` | `2d13567` | Market Engine divergence archaeology |
| 2026-07-13 | Post-REBASE 001 Task 1 — Forensic archaeology of `market_engine.py` divergence | `CORE_CANONICAL_DROPBOX_FREEZE` | Report-only | none | Isolated functional validation |
| 2026-07-13 | Post-REBASE 001 Task 2 — Isolated functional validation of Market Engine release (corrected, 5-scenario) | `CORE_OUTPUT_REQUIRES_OWNER_DECISION` | Report-only | none | Project Owner decision |
| 2026-07-13 | Project Owner Decision — Market Engine rendering divergence | `INTENTIONAL_IMPROVEMENT`; Dropbox source reclassified `HISTORICAL_BEHAVIOUR_ONLY` | `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`, `00_CONFIG/LEGACY_ENGINE_INVENTORY.md` (updated) | `e308c1c` | System Authority Audit |
| 2026-07-13 | Post-REBASE 001 Task 3 — System Authority Audit | Complete; surfaced the untracked `scripts/gmv_*.sh` automation family and the separate `~/GMV_CORE` root | Report-only | none | GMV Engine/Decision Engine archaeology |
| 2026-07-13 | Post-REBASE 001 Task 4 — Forensic archaeology of GMV Engine / Decision Engine chain | `FREEZE_UNAPPROVED_AUTOMATION` | Report-only | none | Project Owner decision |
| 2026-07-13 | Post-REBASE 001 Task 5 — Freeze GMV Engine / Decision Engine automation | Complete; `com.gmv.engine` disabled at both launchd layers | `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md` | `ab71c7e` | Fenix archaeology |
| 2026-07-13 | Post-REBASE 001 Task 6 — Forensic archaeology of Fenix automation | `FREEZE_UNAPPROVED_AUTOMATION` (recommended; not yet authorized) | Report-only | none | Freeze `com.gmv.fenix` (pending approval) |
| 2026-07-13 | Post-REBASE 001 Meta Task — Create Project Operational Ledger | This document created | `00_CONFIG/PROJECT_STATUS.md` | none (not staged/committed per task instruction) | Per Next Approved Task above |
| 2026-07-13 | Meta Task — Establish Permanent Project Governance | Task Completion Protocol added (Rules 6–9, "PROJECT STATUS UPDATE" block) | `00_CONFIG/PROJECT_STATUS.md` | `e29395d` | Create `SYSTEM_MAP.md` |
| 2026-07-13 | Post-REBASE 001 Meta Task — Create `SYSTEM_MAP.md` | Permanent architectural map created from existing evidence only; no new architecture inferred | `00_CONFIG/SYSTEM_MAP.md`, `00_CONFIG/PROJECT_STATUS.md` | `cd239fb` | Per Next Approved Task above |
| 2026-07-13 | Meta Task — Define Stabilization Exit Criteria | `NEAR COMPLETION` — 2 BLOCKER items remain (freeze `com.gmv.fenix`; archaeology/disposition of `~/GMV_CORE`'s two loaded LaunchAgents); all other remaining work classified `POST-STABILIZATION` or `HISTORICAL` | `00_CONFIG/STABILIZATION_EXIT_CRITERIA.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Freeze `com.gmv.fenix` (pending approval); archaeology of `~/GMV_CORE`'s live LaunchAgents |
| 2026-07-13 | Post-REBASE 001 Task 7 — Provisional Suspension of Fenix Pending Technical Review | `PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW` (operational suspension only; no final architectural disposition); `com.gmv.fenix` disabled at both launchd layers; blocker count reduced from 2 to 1 | `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md`, `00_CONFIG/PROJECT_STATUS.md` | `345ca3e` | Forensic archaeology of `~/GMV_CORE`, `com.gmv.morningbrief.email`, `com.gmv.dailybrief` |
| 2026-07-13 | Stabilization — Final Blocker Task: Forensic Archaeology of `~/GMV_CORE` and its two live automations | Report-only. Root: `PROTOTYPE_ROOT` (relationship classification), `PRESERVE_PENDING_REVIEW` (disposition). `morning_os.py`: `PROVISIONALLY_SUSPEND` (7/7 runs failed, exit 1; live external-email capability sharing governed SMTP infrastructure). `dropbox_daily_brief.py`: `PROVISIONALLY_SUSPEND` (6/6 runs failed, `EX_CONFIG` 78, from a confirmed unescaped-`&&` plist XML defect, independently reproduced from the 2026-07-10 archaeology pass). No operational modification performed. | Report-only (no new document created) | not staged/committed, per task instruction | Provisional suspension of `com.gmv.morningbrief.email` and `com.gmv.dailybrief`, pending Project Owner authorization |
| 2026-07-13 | Stabilization — Final Blocker Resolution: Provisional Suspension of `~/GMV_CORE` Live Automations | `PROVISIONALLY SUSPENDED` for both `com.gmv.dailybrief` and `com.gmv.morningbrief.email` (operational suspension only; `~/GMV_CORE` preserved as `PROTOTYPE_ROOT`, `PRESERVE_PENDING_REVIEW`, not classified as legacy). `com.gmv.morningbrief.email` disabled at both plist and launchd-database layers; `com.gmv.dailybrief` disabled at the launchd-database layer only — its plist could not be safely edited due to its pre-existing, unrepaired XML defect (recorded as a limitation, not fixed). Live ungoverned automation blocker count reduced to zero; Stabilization exit criteria operationally satisfied, but Stabilization not yet formally closed. | `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md`, `00_CONFIG/PROJECT_STATUS.md` | `fecfb22` | Formal closure of Stabilization and opening of GMV Core Development |
| 2026-07-13 | Post-REBASE 001 — Final Stabilization Closure | All 8 Stabilization Exit Criteria verified: 6 `PASS`, 2 `PARTIAL` (both explicitly pre-approved as non-blocking by `STABILIZATION_EXIT_CRITERIA.md`). No `BLOCKER` remains. One `DOCUMENTATION_ONLY` inconsistency found and corrected (`SYSTEM_MAP.md` §4's stale Fenix/`~/GMV_CORE` entries). Stabilization formally declared **CLOSED**; future work belongs to GMV Core Development. | `00_CONFIG/STABILIZATION_CLOSED.md`, `00_CONFIG/SYSTEM_MAP.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — GMV Core Development not yet opened by any specific task |
| 2026-07-18 | GMV V2 Sprint 002 Reliability — S002-20 (`AUTO-010`) Sprint Review remediation. A bounded Sprint Review (evidence-only, no file changes) first found `S002-19`/`S002-15`/`S002-16`/`S002-17` complete but `S002-20` failing its own acceptance bar: `gmv status`'s `backup.freshness` check self-reported `UNAVAILABLE`/`"requires S002-20"` despite 1,163+ live verified backups, no isolated restore test had ever been recorded, and OID continuity was unverified. Remediation (Project Owner authorized, scope bounded to four named defects) | Complete. (1) `backup_service.check_freshness()` replaces the hardcoded placeholder in `health_service.collect_health()` — validates the audit chain, checks the latest verified backup against the approved 900s RPO, re-verifies the manifest live, and checks restore-test evidence against the approved 30-day cadence; fails closed on any missing/malformed/stale/unverifiable evidence. (2) `restore_check()` now records a tamper-evident `restore.check` audit event (verified/failed), hash-chained consistently with the existing `backup.create` mechanism. (3) OID continuity (duplicates, malformed, sequence gaps, source-vs-restored comparison) is verified in both `verify_backup()` and `restore_check()`, fail-closed. (4) No automatic restore scheduler was added; the monthly restore test remains an explicitly human-owned manual operation (`gmv snapshot restore-check`), monitored by (1). Full test suite: **137 passed**, 0 failed (`ruff`, `git diff --check`, `pip check`, `bash -n` all clean on changed files). A live isolated restore-check was then run under explicit, scoped, per-operation Project Owner approval: source `~/.gmv_backups/sets/BKP-20260718T162243Z-7882593f`, isolated target `/tmp/gmv-restore-check-S002-20-20260718T162243Z`, read-only core comparison `~/.gmv_core`. Result: verified — manifest/checksum/integrity/foreign-key checks passed, OID continuity clean (12/12, 0 duplicates, 0 malformed) in both backup and restored copies, 0 OIDs missing from source, canonical database confirmed untouched (`09_DATABASE/GMV.db` mtime unchanged by the operation). Audit trail: first-ever `restore.check` record, sequence `1165`, correctly hash-chained onto the existing `backup.create` chain. `gmv status --json` `backup.freshness` now reports **PASS** (previously `UNAVAILABLE`, then `DEGRADED` once backups alone were governed) — first PASS this check has ever produced. Sprint 003 was explicitly not opened. | `10_API/backup_service.py`, `10_API/health_service.py`, `10_API/status_service.py`, `11_CLI/gmv`, `00_CONFIG/HEALTH_POLICY.json`, `tests/recovery/test_backup_service.py`, `tests/observability/test_scheduled_health.py`, `tests/cli/test_strict_shell.py`, `tests/cli/test_evidence_status.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — Sprint 003 not begun; GMV Core Development not yet opened by any specific task |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — Project Owner GO verdict, and its first dependency-ordered task. `GMV_V2_EXECUTION_ROADMAP.md`'s Sprint 003 "Small-step execution sequence" step 1 (`ARC-001`, `ARC-002`, `MAIN-001`, `MAIN-004` — "minimal Core package/repository boundary") could not start directly: `gmv_core/`'s designation as the intended Core package was undecided, and `ARC-001`'s declared `Dependencies` (`DB-002`, `DB-003`) are themselves later Sprint 003 steps, contradicting the roadmap's own step ordering. Reported to the Project Owner rather than resolved unilaterally. | Project Owner resolved both questions: `gmv_core/` is the Core package; migration is one named service per task, never all direct-SQLite callers at once; `DB-002`/`DB-003` block final `ARC-002` enforcement, not initial construction; the first slice is `object_service.py`'s `list`/`show`/`count`, behavior-preserving, no schema change. Recorded in `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`. Implementation: `gmv_core/repositories/objects.py` created (typed functions over an open `sqlite3.Connection`, no lifecycle ownership, matching the existing `gmv_core/repositories/identity.py` convention); `10_API/object_service.py`'s `list_objects()`/`count_objects()`/`show_object()` now delegate to it. Output format, exit codes, and OID-validation-before-database-access ordering are unchanged — confirmed by new characterization tests locking in exact `list`/`count`/`show`/missing-OID CLI output, plus direct repository unit tests, plus a live read-only sanity check against the real database (12 objects, 6 types, unchanged). No schema change. No other `10_API` service migrated (17 of the 18 direct-`sqlite3.connect` call sites are untouched). Full test suite: **145 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`, `gmv_core/repositories/objects.py`, `10_API/object_service.py`, `tests/objects/test_repository.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — second bounded task: move `object_service.py`'s database connection ownership into `gmv_core`, so the script no longer imports `sqlite3` or calls `sqlite3.connect` directly. Preserve all behavior/output; no schema change; no other service migrated; the known `$HOME`-path issue in `11_CLI/gmv`'s `object` subcommand left untouched (separate, out of scope). | Complete. New `gmv_core/database.py` module (`connect(home=None) -> sqlite3.Connection`) resolves the database path via `gmv_core`'s own `load_config()`/`GMVPaths` (previously built in Sprint 001, unused until now) and opens the connection; callers retain `sqlite3.Connection`'s own context-manager semantics (commit/rollback on exit, connection not explicitly closed) — verified directly, not assumed. `object_service.py`'s `import sqlite3`, its hardcoded `DB` path constant, and its own `connect()` function are removed; its three call sites now use `database.connect()`. Confirmed byte-identical default path resolution against the old hardcoded `Path.home()/".gmv_core/09_DATABASE/GMV.db"` in every currently-exercised code path (production shell, both test-fixture styles) — the only new capability is that `object_service.py` now also honors `GMV_HOME` if explicitly set, inert today since nothing sets it outside test fixtures. Added: 5 focused tests for `gmv_core/database.py` (path resolution under an injected home, a live query round-trip, default-precedence resolution against the `isolated_gmv` fixture, explicit preservation of the non-closing context-manager semantics, and `ConfigurationError` propagation when neither `GMV_HOME` nor `HOME` resolves) plus one static boundary test (`tests/objects/test_object_service_boundary.py`) asserting the literal string `"sqlite3"` does not appear anywhere in `object_service.py` — deliberately scoped to this one file only, not the deferred repo-wide `ARC-002` enforcement check. All four existing `object_*` characterization tests and the OID-validation-before-database-access test pass unchanged. Live read-only sanity check against the real database (`gmv object list`/`count`/`show`) confirmed output identical to the prior baseline. Full test suite: **151 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/database.py`, `10_API/object_service.py`, `tests/test_database.py`, `tests/objects/test_object_service_boundary.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — third bounded task: migrate `10_API/service_service.py` behind the Core persistence boundary, the same repository-plus-connection-ownership pattern used for `object_service.py`, since step 1 of the roadmap's execution sequence is an incremental, one-service-per-task process per `ADR_CORE_PERSISTENCE_BOUNDARY.md`. Proposed (goal/files/acceptance criteria/human decision, no file changes) before implementation; Project Owner authorized `service_service.py` specifically as the next slice. | Complete. New `gmv_core/repositories/services.py` (`list_services`, `list_runs`, `get_service`, typed functions over an open `sqlite3.Connection`, matching the `objects.py`/`identity.py` convention) — `list_runs` preserves the existing `service_runs`/`engine_runs` UNION ALL query verbatim, including the `COALESCE`/`NOT EXISTS` de-duplication logic, unchanged. `10_API/service_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()` (the already-existing module, no changes needed). Before editing, exact current CLI output was captured by running the unmigrated script against the characterization schema fixture as ground truth (`list`/`runs`/`show`/missing-OID); after migration, the same invocations were re-run and confirmed byte-identical, avoiding hand-derivation of the UNION ALL query's result order. Added: 4 repository unit tests (`tests/services/test_service_repository.py` — renamed from an initial `test_repository.py` to avoid a pytest module-basename collision with the pre-existing `tests/objects/test_repository.py`, since this repo's test tree has no `__init__.py` packages), 1 static boundary test (`tests/services/test_service_service_boundary.py`, same scoped `"sqlite3" not in source` check as `object_service.py`'s), and 4 new characterization tests locking in exact `list`/`runs`/`show`/missing-OID CLI output. No schema change. No other `10_API` service migrated (16 of the remaining 17 direct-`sqlite3.connect` call sites are untouched). Live read-only sanity check against the real database confirmed working, correctly formatted output. Full test suite: **160 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/services.py`, `10_API/service_service.py`, `tests/services/test_service_repository.py`, `tests/services/test_service_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — fourth bounded task: migrate `10_API/search_service.py` behind the Core persistence boundary, same pattern as the two prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `search_service.py` selected over `timeline_service.py`/`event_service.py` (flagged for future merge under `MAIN-003`), `snapshot_service.py` (already "legacy" per `SNAPSHOT_BACKUP_CONTRACT.md`), and `base_service.py` (unreferenced dead code) — smallest stable, wired, zero-write, single-`connect()` candidate remaining. Project Owner authorized `search_service.py` specifically. | Complete. New `gmv_core/repositories/search.py` (`search(connection, query)`, typed function over an open `sqlite3.Connection`) preserves all four cross-table sub-queries (`objects`, `resources`, `events`, `relations`) verbatim, including the `CASE`/`instr()`/`COALESCE` matching logic and per-table `ORDER BY`. `10_API/search_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its one function now uses `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture across four cases (name/filename match, event-description match, relation-type match, no match) plus the missing-query-argument case, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/search/test_search_repository.py`), 1 static boundary test (`tests/search/test_search_service_boundary.py`, same scoped `"sqlite3" not in source` check), and 5 new characterization tests locking in the four match-category outputs plus the missing-argument usage/exit-code contract. No schema change. No other `10_API` service migrated (15 of the remaining direct-`sqlite3.connect` call sites are untouched). Live read-only sanity check against the real database confirmed correct output. Full test suite: **170 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/search.py`, `10_API/search_service.py`, `tests/search/test_search_repository.py`, `tests/search/test_search_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — fifth bounded task: migrate `10_API/resource_service.py` behind the Core persistence boundary, same pattern as the three prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `resource_service.py` selected as the cleanest remaining candidate — excluded `artifact_audit.py` (already parameterized on `Path`, imported by `doctor_service.py`, different/greater coupling), `event_service.py`/`timeline_service.py` (flagged for merge under `MAIN-003`), `snapshot_service.py` (legacy), `base_service.py`/`plugin_manager.py` (dead code), `plugin_service.py` (part of `MAIN-005` Plugin-duplication retirement), and `backup_service.py`/`status_service.py`/`gmv_compatibility.py`/`import_service.py` (already complex or write-bearing). Project Owner authorized `resource_service.py` specifically. | Complete. New `gmv_core/repositories/resources.py` (`list_resources`, `count_resources`, `get_resource`, typed functions over an open `sqlite3.Connection`, matching the established convention) preserves the `resource_view`/`resources` queries verbatim. `10_API/resource_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture (`list`/`count`/`show`/missing-OID) before editing, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/resources/test_resource_repository.py`), 1 static boundary test (`tests/resources/test_resource_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`count`/`show`/missing-OID CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct output. Full test suite: **179 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/resources.py`, `10_API/resource_service.py`, `tests/resources/test_resource_repository.py`, `tests/resources/test_resource_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — sixth bounded task: migrate `10_API/relation_service.py` behind the Core persistence boundary, same pattern as the four prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `relation_service.py` selected as the last remaining candidate in the same clean list/count/show shape as the four already-migrated services, with no additional coupling — `queue_service.py` noted as a valid alternative but already partially coupled to `gmv_core.errors`/`gmv_core.validation`; `doctor_service.py` excluded for centrality (consumed by `health_service.py`). Project Owner authorized `relation_service.py` specifically. | Complete. New `gmv_core/repositories/relations.py` (`list_relations`, `count_relations`, `get_relations`, typed functions over an open `sqlite3.Connection`, matching the established convention) preserves the `relation_view`/`relations` queries verbatim, including the `source_oid=? OR target_oid=?` matching logic. `10_API/relation_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture (`list`/`count`/`show` as source, `show` as target, missing-OID) before editing, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/relations/test_relation_repository.py`), 1 static boundary test (`tests/relations/test_relation_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`count`/`show`/no-match CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct output. Full test suite: **188 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/relations.py`, `10_API/relation_service.py`, `tests/relations/test_relation_repository.py`, `tests/relations/test_relation_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — seventh and final simple-candidate task: migrate `10_API/queue_service.py` behind the Core persistence boundary. Preceded by a full classification of all 13 remaining direct-`sqlite3.connect` callers against the read-only/homogeneous/low-risk criteria used across this series (exclude write-bearing, legacy/dead-code, `MAIN-003`/`MAIN-005`-bound, central/strongly-coupled services); only `queue_service.py` qualified — no batch of up to 4 could be honestly composed. Project Owner authorized `queue_service.py` as the last simple slice of this phase. | Complete. New `gmv_core/repositories/queue.py` (`list_queue`, `get_queue_entry`, typed functions over an open `sqlite3.Connection`) preserves the `import_queue_view` query and the `pending_only` dynamic-`WHERE` construction verbatim. `10_API/queue_service.py`'s `import sqlite3`, hardcoded `DB` constant, its own `connect()`, and the now-unused `QUEUE_COLUMNS` constant are removed (moved into the repository module); its two functions now use `gmv_core.database.connect()`. The `validate_positive_id(import_id)` call in `show_queue_entry()` still runs before `database.connect()` — confirmed unchanged against the pre-existing `tests/cli/test_cli_validation.py::test_queue_show_rejects_invalid_id_before_database_access`. Ground-truth CLI output was captured from the unmigrated script (`list`/`pending`/`show`/missing-ID/invalid-ID/no-args) before editing, then re-verified byte-identical after migration. A pre-existing `pyproject.toml` `per-file-ignores` entry suppressing ruff `S608` (f-string SQL construction, a false positive against the static `QUEUE_COLUMNS` constant) was relocated from `10_API/queue_service.py` to `gmv_core/repositories/queue.py`, following the code it was scoped to — this is a direct, minimal consequence of the migration, not a scope expansion. Added: 4 repository unit tests (`tests/queue/test_queue_repository.py`), 1 static boundary test (`tests/queue/test_queue_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`pending`/`show`/missing-ID CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct (empty) output, matching the real `import_queue` table's current empty state. Full test suite: **197 passed**, 0 failed (`ruff`, `git diff --check` clean on changed and full-repo files). **This closes the simple-candidate migration series** — the next roadmap gate is Sprint 003 execution-sequence step 2 (`MAIN-011`, JSON/Object identity reconciliation), not started, not scoped. | `gmv_core/repositories/queue.py`, `10_API/queue_service.py`, `tests/queue/test_queue_repository.py`, `tests/queue/test_queue_service_boundary.py`, `tests/characterization/test_cli.py`, `pyproject.toml`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — Sprint 003 execution-sequence step 2 (`MAIN-011`) is the next gate; not proposed, scoped, or authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` verification and preliminary audit (read-only). Verified whether `MAIN-011` was cleanly authorizable as the Sprint 003 execution-sequence's step 2: its declared `Dependencies` (`ARC-004`, `ARC-005`, `DB-020`) in `GMV_V2_BACKLOG.md` contain two circular references — `ARC-005`'s own `Dependencies` include `MAIN-011`; `DB-020`'s own `Dependencies` include `ARC-009`, whose `Dependencies` include `DB-020` — meaning `MAIN-011` cannot be fully closed under its own declared graph without first resolving both cycles. Reported to the Project Owner rather than resolved unilaterally, with the concrete divergence found in this repository (`03_STATE/objects/OBJECT-0000001_GMV.json` vs. SQLite `PER-000001`) cited as evidence the problem is real, not hypothetical. | Project Owner ruled the two cycles block migration/closure only, not evidence collection, and authorized a bounded preliminary audit slice. Every identifier under `03_STATE/objects/*.json` (1 file found: `OBJECT-0000001_GMV.json`) and `02_INDEXES/OBJECT_INDEX.json` (1 declared entry, consistent with the directory, no orphans) was cross-referenced against all 12 rows of the SQLite `objects` table by exact-OID match and by `type`+`name` match, and checked against `OID_CONTRACT.md`'s grammar. Result: `OBJECT-0000001` is format-invalid (`OBJECT` is 6 letters where the contract requires exactly 3; `0000001` is 7 digits where it requires exactly 6) and matches `PER-000001` by `type`/`name` (`Person`, `Giacomo Marco Valerio`) — classified **CONFLICT**: a non-conformant identifier aliasing an existing canonical identity for the same entity. Recorded in new `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md` (Scope/Method/Findings/Mapping Table/Completeness Statement/What This Audit Does Not Resolve sections). No JSON, database, or code was modified. No ADR was written; no architecture was proposed — both explicitly out of scope for this slice. | `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Project Owner decision required: (1) which identifier is authoritative, `OBJECT-0000001` or `PER-000001`; (2) how to sequence `MAIN-011`'s migration around the `ARC-005`↔`MAIN-011` and `DB-020`↔`ARC-009` cycles — same category of decision as `ADR_CORE_PERSISTENCE_BOUNDARY.md`, not yet made |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` canonical-identity migration slice. Project Owner ruled `PER-000001` canonical, `OBJECT-0000001` a documented legacy alias, and authorized migration of only the two already-mapped artifacts, independent of `MAIN-011`'s closing dependencies. Instructed: first search all tracked files for live references to `OBJECT-0000001` outside the two mapped artifacts and stop if any exist; otherwise record the decision in an ADR and migrate. | `git grep -n "OBJECT-0000001"` across all tracked files found matches only in `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md`, `00_CONFIG/PROJECT_STATUS.md` (this ledger's own prior entries), and pre-existing prose in `GMV_TECHNICAL_REVIEW.md`/`GMV_V2_BACKLOG.md`/`GMV_V2_EXECUTION_ROADMAP.md` describing the same known conflict — no code, no other data file. A further check for path-based (not string-literal) consumers of `03_STATE/objects/` or `02_INDEXES/` in `*.py`/`*.sh`/`11_CLI/gmv` found one incidental match, `scripts/check_runtime_git_policy.py`'s generic protected-directory list — unrelated to the identifier itself. No live references found; proceeded. Recorded the decision in new `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md` (Context/Decision/Consequences/References). Migrated `03_STATE/objects/OBJECT-0000001_GMV.json` → `03_STATE/objects/PER-000001_GMV.json` (only the `oid` field changed, all other structure/content byte-for-byte identical) and `02_INDEXES/OBJECT_INDEX.json` (`oid` and `path` updated, `version`/`created_at`/`type`/`name`/`status` unchanged). Both directories are gitignored per `RUNTIME_DATA_GIT_POLICY.md` (`.gitignore` lines 12–13) — the migration is a real filesystem change, confirmed by targeted verification (valid JSON, correct `oid` in both files, index path resolves to the renamed file and the file exists, no remaining `OBJECT-0000001` string in either file, all non-identifier fields preserved, old filename no longer exists), but is not itself trackable in git; only the ADR and this ledger entry are part of the commit. Full test suite: **197 passed**, unchanged (no tracked code touched). `ruff` and `git diff --check` clean. | `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`, `00_CONFIG/PROJECT_STATUS.md` (untracked filesystem migration: `03_STATE/objects/PER-000001_GMV.json`, `02_INDEXES/OBJECT_INDEX.json`) | this commit (see `git log`) | None for this slice — `MAIN-011`'s remaining scope (parallel-write enforcement, `ARC-005` reconciliation) still requires future authorization and remains blocked on the two circular backlog dependencies |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` diagnostic enforcement slice: add a tracked `gmv doctor --strict` check over `03_STATE/objects/*.json`/`02_INDEXES/OBJECT_INDEX.json`. Project Owner authorized a check that fails on non-conformant OID, file/index inconsistency, or `type`+`name` duplication against a differently-numbered SQLite OID, and passes when the runtime files are absent — reusing existing `gmv_core` validators/paths, no writes, no schema change. | Complete. New `gmv_core/json_identity_audit.py` (`audit_json_identities(paths, connection) -> IdentityAuditResult`) implements the three failure categories, reusing `gmv_core.identity.validate_oid` (including its `expected_type` parameter, so a JSON record's declared `type` is also checked against its OID's prefix) and new `gmv_core.paths.GMVPaths` properties (`state`, `state_objects`, `indexes`, `object_index`) added following the existing property pattern. `10_API/doctor_service.py`'s `run_checks()` gained an optional `home: Path \| None = None` parameter (default preserves the prior single-argument call site in `health_service.py` unchanged) and now appends an `identity.json_conformance` check via the existing `_with_database` helper — a nonexistent database (already covered by `test_missing_database_reports_all_dependent_failures`) still fails this check too, since it can't open the required read-only connection, preserving that test's "all checks fail together" assumption without modification. Explicit module and CLI docstring note: this is **diagnostic enforcement only** — it detects and reports a conflict after the fact via `gmv doctor --strict`; it does **not** lock, freeze, or physically prevent a write to either file. `MAIN-011`'s "reject new parallel state writes" requirement (physical prevention) is a distinct, still-unimplemented, unauthorized requirement. Added: 5 new tests in `tests/identity/test_json_identity_audit.py` (absence-PASS, conformant-PASS, and one FAIL test per error category), 2 new path-property assertions in `tests/test_foundation.py`, and updated `tests/cli/test_strict_doctor.py`'s exact-check-list assertion (the only existing test affected — verified by running the full existing suite before writing this test's fix, confirming the other three `test_strict_doctor.py` tests already passed unmodified). Live check against the real repository confirmed `PASS`, validating the previously-migrated `PER-000001_GMV.json` end-to-end. Full test suite: **202 passed**, 0 failed (`ruff`, `git diff --check` clean). | `gmv_core/paths.py`, `gmv_core/json_identity_audit.py`, `10_API/doctor_service.py`, `tests/identity/test_json_identity_audit.py`, `tests/test_foundation.py`, `tests/cli/test_strict_doctor.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — `MAIN-011`'s physical write-prevention requirement and the broader `ARC-005` reconciliation remain future, separately-authorized work |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` governed-freeze declaration. Instructed to verify writer existence across the whole `~/.gmv_core` tree (tracked, untracked, gitignored) and every `com.gmv.*` LaunchAgent target script, without archaeology outside `~/.gmv_core` and its LaunchAgents, and to record findings as a governance addendum — no code, no new architecture, `ARC-005`/`DB-020`/`ARC-009` not to be declared resolved. | Complete. Exhaustive `grep` across tracked/untracked/gitignored files (including `.gmv_runtime/`, `99_SYSTEM/`, `bin/`, `env/`, `00_CONFIG/lce/`) and `launchctl print`/`print-disabled` checks on every `com.gmv.*` agent's target script found zero writers to `03_STATE/objects/*.json` or `02_INDEXES/OBJECT_INDEX.json` — only the read-only diagnostic check from the prior slice and prose documentation. `com.gmv.morningbrief.email`'s target (`~/GMV_CORE/06_RUNNER/morning_os.py`) was checked as a LaunchAgent but not traversed further, per the explicit external-root exclusion; it is disabled regardless. Recorded as an addendum to `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md` (dated 2026-07-19): both artifacts declared runtime-derived and non-authoritative; no writer authorized; any future writer requires explicit Project Owner approval and must route through `gmv_core.identity`/`gmv_core.repositories.identity`. The addendum explicitly separates three concepts that must not be conflated — governed freeze (this declaration, policy-level, no enforcing code), diagnostic enforcement (the existing `identity.json_conformance` check, after-the-fact only), and filesystem immutability (does not exist — no technical control blocks a write). `ARC-005`, `DB-020`, `ARC-009` explicitly not declared resolved. No code added. | `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — `MAIN-011`'s physical write-prevention mechanism, if ever authorized, and the `ARC-005`/`DB-020`/`ARC-009` cycles remain future, separately-authorized work |

---

## 5. Document Index

*Canonical documents only, grouped by category. This is an index, not a copy — read the referenced file for full content.*

**ADR**
- `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`
- `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`
- `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`

**Freeze**
- `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`
- `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`
- `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`
- `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md`

**Suspension** (operational only — not a Freeze/legacy classification; final disposition pending)
- `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md`
- `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md`

**Specification**
- `00_CONFIG/SERVICE_SPECIFICATION.md`
- `00_CONFIG/LEGACY_ENGINE_INVENTORY.md`
- `00_CONFIG/OID_CONTRACT.md`

**Architecture**
- `00_CONFIG/GMV_CORE_ARCHITECTURE.md`
- `00_CONFIG/SOURCE_RUNTIME_BOUNDARIES.md`
- `00_CONFIG/RUNTIME_DATA_GIT_POLICY.md`
- `00_CONFIG/LOCAL_CODING_ENGINE.md` (experimental scope)
- `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md` (`MAIN-011` preliminary audit — evidence only, no migration decision)

**Status**
- `00_CONFIG/PROJECT_STATUS.md` (this document)
- `00_CONFIG/REBASE_001_CLOSED.md`
- `00_CONFIG/REBASE_001_INTERIM_STATE.md`
- `00_CONFIG/WHAT_TO_DO_NEXT.md`
- `00_CONFIG/SYSTEM_MAP.md` (permanent architectural map, not a status document — indexed here for discoverability)
- `00_CONFIG/STABILIZATION_EXIT_CRITERIA.md` (governance document — exit conditions for the Stabilization phase)
- `00_CONFIG/STABILIZATION_CLOSED.md` (closure record — Stabilization phase formally closed)

Additional pre-existing governance documents (security, retention, backup, versioning, and per-domain architecture references) exist under `00_CONFIG/` outside the scope of REBASE 001 / Post-REBASE 001 and are not indexed here.

---

## 6. Open Questions

*Intentionally unresolved. Not blockers unless stated in §1.*

1. Root cause of the persistent `gmv status` CLI failure observed under `launchd` execution (Post-REBASE 001 Tasks 4 and 6 — occurs even when the environment is correctly sourced, per Task 6).
2. Whether `gmv_snapshot.sh` has been silently failing since 2026-07-07 (confirmed: no new snapshot file created since that date; root cause not diagnosed — Post-REBASE 001 Task 6).
3. Classification of Dropbox `property_engine.py` (open since REBASE 001 Task 3; never resolved).
4. Correction: `~/GMV_CORE` was earlier described in this ledger as the "Local Coding Engine workspace root." This task (Stabilization Final Blocker Task) found `~/GMV_CORE`'s own `README.md` self-identifies it as "GMV RESEARCH LAB," an experimental prototyping lab — a distinct stated purpose from `00_CONFIG/LOCAL_CODING_ENGINE.md`'s "Local Coding Engine v0" concept. No evidence directly links the two; the earlier conflation in this ledger was not evidence-based and is corrected here. `~/GMV_CORE`'s root-level disposition is `PRESERVE_PENDING_REVIEW`; its two live LaunchAgents have a separate, resolved verdict (`PROVISIONALLY_SUSPEND` each) pending execution — see the Stabilization Final Blocker Task report.
5. Disposition of `gmv_watchdog.sh` and `gmv_orchestrator.sh` — flagged as sharing the same governance-contradiction evidence as the frozen GMV Engine and archaeologized Fenix, not yet individually archaeologized.
6. Fenix's final architectural disposition (keep/govern, merge, or decommission) — operationally suspended pending technical review (Post-REBASE 001 Task 7); see `FENIX_PROVISIONAL_SUSPENSION.md` for the minimum requirements that must be satisfied before this question can be resolved.
7. `com.gmv.dailybrief`'s and `com.gmv.morningbrief.email`'s final architectural disposition (keep/govern, merge, or decommission) — operationally suspended pending technical review (Stabilization Final Blocker Resolution); see `GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md` for the minimum requirements that must be satisfied before this question can be resolved.
8. Whether `com.gmv.morningbrief.email`'s dormant external-email capability (to `gmv@area35artgallery.com`, via the same local SMTP relay used by `SRV-000002`) was ever actually exercised during its one manual test run on 2026-07-07 — unconfirmed either way; the capability remains preserved but inactive under suspension.
9. The origin and Dropbox-authenticity of `04_RESULTS/dropbox_decisions.json` (consumed by both `~/GMV_CORE` runners) was not verified — out of scope for the Stabilization Final Blocker Task.

---

## 7. Project Health

*Each an explicit qualitative status, evidenced by the referenced work.*

- **Architecture:** STABLE — core principles (Runtime belongs to Core; Dropbox is Repository; freeze before deletion; authority before execution; archaeology before implementation) established in REBASE 001 and consistently applied through Post-REBASE 001.
- **Governance:** IMPROVING — REBASE 001 resolved four long-standing ungoverned components; a previously-undiscovered automation family (`scripts/gmv_*.sh` plus `~/GMV_CORE`'s two LaunchAgents) was surfaced by the System Authority Audit; every live instance of it has now been suspended or frozen — no live, ungoverned, scheduled automation remains loaded.
- **Repository:** CLEAN — no unexplained tracked modifications as of the last audit (Post-REBASE 001 Task 3).
- **Runtime:** GOVERNED — one native service and three compatibility services are registered and documented; the previously-discovered ungoverned automation family is now fully frozen or suspended (none remains loaded/scheduled).
- **Compatibility:** STABLE — three compatibility services registered, hash-pinned, and documented; one output-rendering divergence formally resolved by ADR.
- **Legacy Review:** IN PROGRESS — four components frozen with documented reactivation conditions; `property_engine.py` remains unclassified; `~/GMV_CORE`'s root is now archaeologized (`PRESERVE_PENDING_REVIEW`) but its ~40 further scripts beyond the two audited (and now suspended) LaunchAgents remain unexamined.
- **Automation Review:** COMPLETE for all previously-live, scheduled automation — System Authority Audit found 12 untracked `scripts/gmv_*.sh` scripts across 6 LaunchAgents plus 2 further live automations in `~/GMV_CORE`; 2 scripts (`gmv_engine.sh`, `gmv_decision_engine.sh`) are frozen, 3 (`gmv_fenix_engine.sh`, `morning_os.py`, `dropbox_daily_brief.py`) are provisionally suspended; 3 remain unaudited but were already dormant/unloaded at last check (`gmv_watchdog.sh`, `gmv_orchestrator.sh`, and their further sub-scripts) — none of these three is currently live.
- **Development Readiness:** READY FOR CONTROLLED WORK — the evidence-first, report-then-freeze pattern is well-established, repeatable, and has produced consistent, reversible outcomes across every component examined to date; no blocking technical issue prevents continued work.

---

## 8. Next Session Bootstrap

**This is the mandatory session entry point.**

Future sessions must:

1. Read this document (`00_CONFIG/PROJECT_STATUS.md`) in full.
2. Read only the documents listed under **Required Context** below.
3. Continue from **Next Approved Task** (§1). If none is set, treat the
   highest-priority `PENDING_APPROVAL` item in §3 as the item requiring a
   Project Owner decision before work resumes — do not begin it unilaterally.

Do not reconstruct context from unrelated documents unless explicitly
instructed by the user.

**Required Context:**

- `00_CONFIG/REBASE_001_CLOSED.md`
- `00_CONFIG/WHAT_TO_DO_NEXT.md`
- `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`
- `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`
- `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`
- `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`
- `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md`
- `00_CONFIG/SERVICE_SPECIFICATION.md`
- `00_CONFIG/LEGACY_ENGINE_INVENTORY.md`
