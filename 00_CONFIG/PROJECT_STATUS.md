# GMV Project Status — Operational Ledger

## Operational Rules

1. `PROJECT_STATUS.md` is the single operational ledger for this project.
2. Every completed task updates `PROJECT_STATUS.md`.
3. No task is considered complete until `PROJECT_STATUS.md` has been updated.
4. `PROJECT_STATUS.md` records facts only. No speculation. No architectural
   opinions. No implementation proposals.
5. Historical records remain append-only. Corrections are added as new
   entries. Previous history is never silently rewritten.
6. Every future task, without exception, must end by updating
   `PROJECT_STATUS.md`, appending the fixed "PROJECT STATUS UPDATE" block
   defined in the Task Completion Protocol below.
7. `PROJECT_STATUS.md` is the unique operational source of truth for
   current project state. ADRs remain the authoritative architectural
   decision records; freeze documents remain the authoritative component
   lifecycle records. This document indexes and summarizes them — it never
   replaces or restates their content.
8. Historical reports (archaeology reports, task outputs) are immutable
   once completed and are never edited after completion. Corrections take
   the form of new entries, per Rule 5.
9. Every future task must verify that `PROJECT_STATUS.md` has been updated
   before declaring the task complete.

This document is not an ADR, not an archaeology report, and not a handoff.
It is a living ledger. ADRs, freeze records, and archaeology reports remain
the source of full reasoning; this document only tracks current state,
active decisions, and the chronological record of what happened.

---

## Task Completion Protocol

Every future task must end its own output with a fixed "PROJECT STATUS
UPDATE" block, and must reflect that same information here (§1 Dashboard,
and §4 Timeline for the completed entry) before the task is declared
complete.

Required block, verbatim field set:

```
## PROJECT STATUS UPDATE

- Current Phase: <...>
- Current Approved Task: <...>
- Tasks Completed: <...>
- Architecture Changed (YES/NO): <...>
- ADR Created: <...>
- Freeze Created: <...>
- Documents Updated: <...>
- Next Approved Task: <...>
- Known Risks: <...>
- Working Tree Status: <...>
- Commit: <...>
```

A task is not complete until this block has been produced in the task's
own report and `PROJECT_STATUS.md` has been updated to match it.

---

## 1. Project Dashboard

*Current state only. For history, see §4. For architectural reasoning, see §2 and the referenced documents.*

- **Current Phase:** GMV Core Development. Stabilization is formally closed (`00_CONFIG/STABILIZATION_CLOSED.md`).
- **Current Objective:** Sprint 003 execution-sequence step 6 remains in progress. `DB-002` is complete and the isolated `DB-003` non-queue enforcement slice is prepared as explicit migration 007. Live and the application default remain v6; dependent DB-008 follows only after the separately approved DB-003 cutover. Queue lifecycle/confidence persistence remain DB-010-owned and duplicated lifecycle statuses remain DB-013-owned.
- **Last Completed Task:** Project Owner-authorized isolated design and testing of migration 007. The explicit v6-to-v7 target preflights lexical Object OIDs, exact Service Run outcomes, integer compatibility mode, and non-self Relations before DDL; atomically rebuilds only `objects`, `service_runs`, `engines`, and `relations`; preserves all rows, restrictive foreign keys, dependent views, Event triggers, indexes, and sequences; verifies `foreign_key_check`; and advances `user_version` only on commit. Four negative preflights and two injected fault points prove exact rollback to v6 plus restoration of `foreign_keys=1`. Positive and negative writes cover every constraint. Deferred lifecycle status values and Queue state/confidence remain unconstrained and are preserved. Migration/static tests **68 passed**; full suite **356 passed**. A byte-copy of the real live shape migrated to v7 with integrity OK, zero FK violations, and complete table/view/trigger/sequence/FK parity. Live remained byte-identical v6 at SHA-256 `131245ce...5735`. No backup, cutover, restore, live write, or scheduler operation.
- **Next Approved Task:** None formally approved. The next canonical gate is controlled live DB-003 preflight, verified milestone backup, and conditional migration 007 cutover; default promotion to v7 occurs only after successful post-cutover verification.
- **Last Commit:** this task's commit (see `git log`) — "feat: add isolated DB-003 domain migration".
- **Repository Status:** Clean — no unexplained tracked modifications as of the last System Authority Audit (Post-REBASE 001 Task 3, 2026-07-13). Untracked material remains present (see §7, Automation Review) but is inventoried, not unexplained.
- **Runtime Status:** One native Core service (`SRV-000001` Knowledge Engine) and three compatibility services (`SRV-000002` Morning Brief, `SRV-000003` Daily Log, `SRV-000004` Market Engine) are registered and active. Four components are formally frozen (Apprentice, Constitution CLI feature, Real Estate orchestration, GMV Engine/Decision Engine automation). Fenix, `com.gmv.dailybrief`, and `com.gmv.morningbrief.email` are provisionally suspended pending technical review (operational suspension only — none classified as legacy/frozen/rejected). No live, ungoverned, scheduled automation remains loaded. A scheduled backup automation (`com.gmv.backup`, 15-minute interval) runs a governed, verified full-system backup with a hash-chained audit trail; isolated restore verification is a human-owned manual operation (`gmv snapshot restore-check`), not scheduled.
- **Current Risks:** See §6, Open Questions and §7, Project Health.
- **Current Blockers:** None. Explicit Project Owner authorization is required for the milestone backup and conditional live migration 007 gate.
- **Development Readiness:** Ready — Stabilization is closed; GMV Core Development may begin once a task is explicitly opened.
- **Last Updated:** 2026-07-19.

---

## 2. Permanent Architectural Decisions

*Only active decisions. Full reasoning lives in the referenced ADRs/freeze records — never duplicated here.*

| Category | Decision | Reference |
|---|---|---|
| **Repository** | Dropbox (`GMV_MASTER_SYSTEM`) is the canonical Repository for non-runtime material; it is never the canonical source for executable runtime code. | `00_CONFIG/REBASE_001_CLOSED.md` §"Major Architectural Decisions" |
| **Runtime** | Runtime code belongs to GMV Core (`~/.gmv_core`, git-tracked). | `00_CONFIG/REBASE_001_CLOSED.md` §"Major Architectural Decisions" |
| **Compatibility** | Legacy engines execute through a hash-pinned Compatibility Layer (`10_API/gmv_compatibility.py`) with fail-closed release verification. Registered services: `SRV-000002` Morning Brief, `SRV-000003` Daily Log, `SRV-000004` Market Engine. | `00_CONFIG/SERVICE_SPECIFICATION.md`, `00_CONFIG/LEGACY_ENGINE_INVENTORY.md` |
| **Legacy** | Frozen, preserved, not deleted: Apprentice; Constitution CLI feature; Real Estate orchestration (`realestate_runner.py` family); Dropbox `market_engine.py` (reclassified `HISTORICAL_BEHAVIOUR_ONLY`); GMV Engine/Decision Engine automation. | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`, `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`, `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`, `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`, `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md` |
| **Experimental** | A Local Coding Engine (v0, Draft status) exists as a separate, explicitly-scoped, read-only-first initiative. | `00_CONFIG/LOCAL_CODING_ENGINE.md` |
| **Primary Engineering** | Knowledge Engine (`SRV-000001`, `01_RUNTIME/knowledge_engine.py`) is the native Core runtime and the successor to the frozen Apprentice concept. | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` |
| **Independent Validation** | Divergent implementations are validated via isolated, `mktemp`-based fixture execution — never against production data — before any output-contract decision is made. | Post-REBASE 001 Task 2 (methodology; report only, no standalone document) |
| **Core Package** | `gmv_core/` (created Sprint 001) is the designated target package for `ARC-001`/`MAIN-001` ("executable Core"). Migration onto it is incremental, one named service per task. `ARC-002`'s static enforcement check (rejecting direct `sqlite3.connect` outside `gmv_core`) is deferred until `DB-002`/`DB-003` (foreign keys, domain constraints) land — it blocks final enforcement, not initial construction. | `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md` |
| **Events** | Events are strictly append-only. Corrections/supersessions are new Events with an explicit reference to the immutable Event they supersede. | `00_CONFIG/ADR_DB004_APPEND_ONLY_EVENTS.md` |
| **Engine/Service identity** | `service_runs` is the canonical execution ledger; the four governed Engine identities map to `SRV-000001…004`. Exact historical duplicates are retained once; mapped missing rows migrated once; the frozen Constitution CLI anomaly is preserved as evidence without a Service OID. Schema v5 retired the `engine_runs` table after atomic parity verification. | `00_CONFIG/ADR_DB006_ENGINE_SERVICE_RECONCILIATION.md` |
| **Foreign keys** | Ten canonical references are enforced with `ON UPDATE RESTRICT ON DELETE RESTRICT`; all ordinary SQLite connections enable and verify enforcement. Only migrations 006 and 007 may temporarily disable it for their confined atomic table rebuilds, with fail-closed restoration. | `00_CONFIG/ADR_DB002_RESTRICTIVE_FOREIGN_KEYS.md` |
| **Import Queue state** | One future `state` field owns the lifecycle; exact transitions separate processing, human review, retryable/terminal failure, and custody. The legacy `status`/`review_status` pair is not a dual authority. | `00_CONFIG/ADR_DB003_IMPORT_QUEUE_STATE_MACHINE.md` |
| **Domain constraint scope** | DB-003 owns only lexical OID grammar, Service Run outcomes, compatibility mode, confidence range, and non-self Relations. DB-008 owns OID prefix/type semantics; DB-010 owns Queue persistence; DB-013 owns duplicated lifecycle statuses and their future checks. | `00_CONFIG/ADR_DB003_AUTHORITATIVE_DOMAIN_SCOPE.md` |

---

## 3. Active Work

*Current work queue. Status reflects actual authorization state, not aspiration.*

| Item | Status | Owner | Priority | Dependencies |
|---|---|---|---|---|
| Fenix final disposition (`com.gmv.fenix` / `gmv_fenix_engine.sh`) | PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW (Post-REBASE 001 Task 7; see `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md` for minimum requirements) | Unassigned | Medium (operational exposure removed; final disposition not urgent) | None |
| `com.gmv.dailybrief` / `com.gmv.morningbrief.email` final disposition | PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW (this task; see `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md` for minimum requirements) | Unassigned | Medium (operational exposure removed; final disposition not urgent) | None |
| `~/GMV_CORE` root — broader classification beyond its two (now suspended) live LaunchAgents | IDENTIFIED, unscheduled; root verdict `PRESERVE_PENDING_REVIEW`; ~40 further scripts and the `GMV_REDUCED` subsystem not yet audited | Unassigned | Low | None |
| Classify Dropbox `property_engine.py` | IDENTIFIED, unscheduled (open since REBASE 001 Task 3) | Unassigned | Medium | None |
| Archaeology of `gmv_watchdog.sh` and `gmv_orchestrator.sh` | IDENTIFIED, unscheduled (flagged by System Authority Audit and GMV Engine/Fenix archaeology as sharing the same governance gap) | Unassigned | Medium | None |
| Diagnose persistent `gmv status` CLI failure under `launchd` | IDENTIFIED, unscheduled (evidence gap in Post-REBASE 001 Tasks 4 and 6) | Unassigned | Medium | None |

---

## 4. Timeline

*Append-only. Never rewritten. Corrections are added as new entries below, not edits to existing ones.*

| Date | Task | Decision | Documents | Commit | Next task |
|---|---|---|---|---|---|
| 2026-07-12 | REBASE 001 Tasks 1–4 (Core migration scope; Dropbox executable classification; duplicate/divergent resolution; Apprentice authority formalization, revised) | Governing principles established: Runtime belongs to Core, Dropbox is Repository | Report-only | none | Migrate Apprentice runtime |
| 2026-07-12 | REBASE 001 Task 5 — Migrate Apprentice runtime into git-tracked Core | Complete | `01_RUNTIME/apprentice/apprentice_runtime.py` | `90d1942` | Apprentice complete archaeology |
| 2026-07-12 | Apprentice Complete Archaeology and Conditional Patching (unnumbered) | `FREEZE_AS_LEGACY` | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` | `113cd1f` | Update freeze doc with launchd disable detail |
| 2026-07-12 | Apprentice freeze record update (launchd persistent-disable documentation) | `FREEZE_AS_LEGACY` (unchanged, documented more fully) | `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md` | `fd1b851` | doctor_service.py archaeology |
| 2026-07-12 | REBASE 001 Tasks 6–7 — Forensic archaeology and revert of `doctor_service.py` | `SAFE_TO_REVERT`; reverted to `HEAD` | Report-only; no commit (file returned byte-identical to `HEAD`) | none | `11_CLI/gmv` archaeology |
| 2026-07-12 | REBASE 001 Tasks 8–9 — Forensic archaeology and revert of `11_CLI/gmv` (Constitution CLI feature) | `FREEZE_UNAPPROVED_FEATURE`; CLI wiring reverted to `HEAD` | Report-only; no commit (file returned byte-identical to `HEAD`) | none | Document Constitution CLI freeze |
| 2026-07-12 | REBASE 001 Task 10 — Document Constitution CLI feature freeze | Complete | `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md` | `e8e9459` | Interim architectural report |
| 2026-07-12 | REBASE 001 Task 11 — Interim architectural report | Complete | `00_CONFIG/REBASE_001_INTERIM_STATE.md` | `a2102a5` | `realestate_runner.py` archaeology |
| 2026-07-12 | REBASE 001 Task 12 — Forensic archaeology of `realestate_runner.py` | `FREEZE_AS_LEGACY` | Report-only | none | Document Real Estate legacy freeze |
| 2026-07-12 | REBASE 001 Task 13 — Document Real Estate legacy freeze | Complete | `00_CONFIG/REALESTATE_LEGACY_FREEZE.md` | `ac17e9b` | Close REBASE 001 |
| 2026-07-12 | REBASE 001 Closure | Complete | `00_CONFIG/REBASE_001_CLOSED.md`, `00_CONFIG/WHAT_TO_DO_NEXT.md` | `2d13567` | Market Engine divergence archaeology |
| 2026-07-13 | Post-REBASE 001 Task 1 — Forensic archaeology of `market_engine.py` divergence | `CORE_CANONICAL_DROPBOX_FREEZE` | Report-only | none | Isolated functional validation |
| 2026-07-13 | Post-REBASE 001 Task 2 — Isolated functional validation of Market Engine release (corrected, 5-scenario) | `CORE_OUTPUT_REQUIRES_OWNER_DECISION` | Report-only | none | Project Owner decision |
| 2026-07-13 | Project Owner Decision — Market Engine rendering divergence | `INTENTIONAL_IMPROVEMENT`; Dropbox source reclassified `HISTORICAL_BEHAVIOUR_ONLY` | `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`, `00_CONFIG/LEGACY_ENGINE_INVENTORY.md` (updated) | `e308c1c` | System Authority Audit |
| 2026-07-13 | Post-REBASE 001 Task 3 — System Authority Audit | Complete; surfaced the untracked `scripts/gmv_*.sh` automation family and the separate `~/GMV_CORE` root | Report-only | none | GMV Engine/Decision Engine archaeology |
| 2026-07-13 | Post-REBASE 001 Task 4 — Forensic archaeology of GMV Engine / Decision Engine chain | `FREEZE_UNAPPROVED_AUTOMATION` | Report-only | none | Project Owner decision |
| 2026-07-13 | Post-REBASE 001 Task 5 — Freeze GMV Engine / Decision Engine automation | Complete; `com.gmv.engine` disabled at both launchd layers | `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md` | `ab71c7e` | Fenix archaeology |
| 2026-07-13 | Post-REBASE 001 Task 6 — Forensic archaeology of Fenix automation | `FREEZE_UNAPPROVED_AUTOMATION` (recommended; not yet authorized) | Report-only | none | Freeze `com.gmv.fenix` (pending approval) |
| 2026-07-13 | Post-REBASE 001 Meta Task — Create Project Operational Ledger | This document created | `00_CONFIG/PROJECT_STATUS.md` | none (not staged/committed per task instruction) | Per Next Approved Task above |
| 2026-07-13 | Meta Task — Establish Permanent Project Governance | Task Completion Protocol added (Rules 6–9, "PROJECT STATUS UPDATE" block) | `00_CONFIG/PROJECT_STATUS.md` | `e29395d` | Create `SYSTEM_MAP.md` |
| 2026-07-13 | Post-REBASE 001 Meta Task — Create `SYSTEM_MAP.md` | Permanent architectural map created from existing evidence only; no new architecture inferred | `00_CONFIG/SYSTEM_MAP.md`, `00_CONFIG/PROJECT_STATUS.md` | `cd239fb` | Per Next Approved Task above |
| 2026-07-13 | Meta Task — Define Stabilization Exit Criteria | `NEAR COMPLETION` — 2 BLOCKER items remain (freeze `com.gmv.fenix`; archaeology/disposition of `~/GMV_CORE`'s two loaded LaunchAgents); all other remaining work classified `POST-STABILIZATION` or `HISTORICAL` | `00_CONFIG/STABILIZATION_EXIT_CRITERIA.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Freeze `com.gmv.fenix` (pending approval); archaeology of `~/GMV_CORE`'s live LaunchAgents |
| 2026-07-13 | Post-REBASE 001 Task 7 — Provisional Suspension of Fenix Pending Technical Review | `PROVISIONALLY SUSPENDED — PENDING TECHNICAL REVIEW` (operational suspension only; no final architectural disposition); `com.gmv.fenix` disabled at both launchd layers; blocker count reduced from 2 to 1 | `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md`, `00_CONFIG/PROJECT_STATUS.md` | `345ca3e` | Forensic archaeology of `~/GMV_CORE`, `com.gmv.morningbrief.email`, `com.gmv.dailybrief` |
| 2026-07-13 | Stabilization — Final Blocker Task: Forensic Archaeology of `~/GMV_CORE` and its two live automations | Report-only. Root: `PROTOTYPE_ROOT` (relationship classification), `PRESERVE_PENDING_REVIEW` (disposition). `morning_os.py`: `PROVISIONALLY_SUSPEND` (7/7 runs failed, exit 1; live external-email capability sharing governed SMTP infrastructure). `dropbox_daily_brief.py`: `PROVISIONALLY_SUSPEND` (6/6 runs failed, `EX_CONFIG` 78, from a confirmed unescaped-`&&` plist XML defect, independently reproduced from the 2026-07-10 archaeology pass). No operational modification performed. | Report-only (no new document created) | not staged/committed, per task instruction | Provisional suspension of `com.gmv.morningbrief.email` and `com.gmv.dailybrief`, pending Project Owner authorization |
| 2026-07-13 | Stabilization — Final Blocker Resolution: Provisional Suspension of `~/GMV_CORE` Live Automations | `PROVISIONALLY SUSPENDED` for both `com.gmv.dailybrief` and `com.gmv.morningbrief.email` (operational suspension only; `~/GMV_CORE` preserved as `PROTOTYPE_ROOT`, `PRESERVE_PENDING_REVIEW`, not classified as legacy). `com.gmv.morningbrief.email` disabled at both plist and launchd-database layers; `com.gmv.dailybrief` disabled at the launchd-database layer only — its plist could not be safely edited due to its pre-existing, unrepaired XML defect (recorded as a limitation, not fixed). Live ungoverned automation blocker count reduced to zero; Stabilization exit criteria operationally satisfied, but Stabilization not yet formally closed. | `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md`, `00_CONFIG/PROJECT_STATUS.md` | `fecfb22` | Formal closure of Stabilization and opening of GMV Core Development |
| 2026-07-13 | Post-REBASE 001 — Final Stabilization Closure | All 8 Stabilization Exit Criteria verified: 6 `PASS`, 2 `PARTIAL` (both explicitly pre-approved as non-blocking by `STABILIZATION_EXIT_CRITERIA.md`). No `BLOCKER` remains. One `DOCUMENTATION_ONLY` inconsistency found and corrected (`SYSTEM_MAP.md` §4's stale Fenix/`~/GMV_CORE` entries). Stabilization formally declared **CLOSED**; future work belongs to GMV Core Development. | `00_CONFIG/STABILIZATION_CLOSED.md`, `00_CONFIG/SYSTEM_MAP.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — GMV Core Development not yet opened by any specific task |
| 2026-07-18 | GMV V2 Sprint 002 Reliability — S002-20 (`AUTO-010`) Sprint Review remediation. A bounded Sprint Review (evidence-only, no file changes) first found `S002-19`/`S002-15`/`S002-16`/`S002-17` complete but `S002-20` failing its own acceptance bar: `gmv status`'s `backup.freshness` check self-reported `UNAVAILABLE`/`"requires S002-20"` despite 1,163+ live verified backups, no isolated restore test had ever been recorded, and OID continuity was unverified. Remediation (Project Owner authorized, scope bounded to four named defects) | Complete. (1) `backup_service.check_freshness()` replaces the hardcoded placeholder in `health_service.collect_health()` — validates the audit chain, checks the latest verified backup against the approved 900s RPO, re-verifies the manifest live, and checks restore-test evidence against the approved 30-day cadence; fails closed on any missing/malformed/stale/unverifiable evidence. (2) `restore_check()` now records a tamper-evident `restore.check` audit event (verified/failed), hash-chained consistently with the existing `backup.create` mechanism. (3) OID continuity (duplicates, malformed, sequence gaps, source-vs-restored comparison) is verified in both `verify_backup()` and `restore_check()`, fail-closed. (4) No automatic restore scheduler was added; the monthly restore test remains an explicitly human-owned manual operation (`gmv snapshot restore-check`), monitored by (1). Full test suite: **137 passed**, 0 failed (`ruff`, `git diff --check`, `pip check`, `bash -n` all clean on changed files). A live isolated restore-check was then run under explicit, scoped, per-operation Project Owner approval: source `~/.gmv_backups/sets/BKP-20260718T162243Z-7882593f`, isolated target `/tmp/gmv-restore-check-S002-20-20260718T162243Z`, read-only core comparison `~/.gmv_core`. Result: verified — manifest/checksum/integrity/foreign-key checks passed, OID continuity clean (12/12, 0 duplicates, 0 malformed) in both backup and restored copies, 0 OIDs missing from source, canonical database confirmed untouched (`09_DATABASE/GMV.db` mtime unchanged by the operation). Audit trail: first-ever `restore.check` record, sequence `1165`, correctly hash-chained onto the existing `backup.create` chain. `gmv status --json` `backup.freshness` now reports **PASS** (previously `UNAVAILABLE`, then `DEGRADED` once backups alone were governed) — first PASS this check has ever produced. Sprint 003 was explicitly not opened. | `10_API/backup_service.py`, `10_API/health_service.py`, `10_API/status_service.py`, `11_CLI/gmv`, `00_CONFIG/HEALTH_POLICY.json`, `tests/recovery/test_backup_service.py`, `tests/observability/test_scheduled_health.py`, `tests/cli/test_strict_shell.py`, `tests/cli/test_evidence_status.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — Sprint 003 not begun; GMV Core Development not yet opened by any specific task |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — Project Owner GO verdict, and its first dependency-ordered task. `GMV_V2_EXECUTION_ROADMAP.md`'s Sprint 003 "Small-step execution sequence" step 1 (`ARC-001`, `ARC-002`, `MAIN-001`, `MAIN-004` — "minimal Core package/repository boundary") could not start directly: `gmv_core/`'s designation as the intended Core package was undecided, and `ARC-001`'s declared `Dependencies` (`DB-002`, `DB-003`) are themselves later Sprint 003 steps, contradicting the roadmap's own step ordering. Reported to the Project Owner rather than resolved unilaterally. | Project Owner resolved both questions: `gmv_core/` is the Core package; migration is one named service per task, never all direct-SQLite callers at once; `DB-002`/`DB-003` block final `ARC-002` enforcement, not initial construction; the first slice is `object_service.py`'s `list`/`show`/`count`, behavior-preserving, no schema change. Recorded in `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`. Implementation: `gmv_core/repositories/objects.py` created (typed functions over an open `sqlite3.Connection`, no lifecycle ownership, matching the existing `gmv_core/repositories/identity.py` convention); `10_API/object_service.py`'s `list_objects()`/`count_objects()`/`show_object()` now delegate to it. Output format, exit codes, and OID-validation-before-database-access ordering are unchanged — confirmed by new characterization tests locking in exact `list`/`count`/`show`/missing-OID CLI output, plus direct repository unit tests, plus a live read-only sanity check against the real database (12 objects, 6 types, unchanged). No schema change. No other `10_API` service migrated (17 of the 18 direct-`sqlite3.connect` call sites are untouched). Full test suite: **145 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`, `gmv_core/repositories/objects.py`, `10_API/object_service.py`, `tests/objects/test_repository.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — second bounded task: move `object_service.py`'s database connection ownership into `gmv_core`, so the script no longer imports `sqlite3` or calls `sqlite3.connect` directly. Preserve all behavior/output; no schema change; no other service migrated; the known `$HOME`-path issue in `11_CLI/gmv`'s `object` subcommand left untouched (separate, out of scope). | Complete. New `gmv_core/database.py` module (`connect(home=None) -> sqlite3.Connection`) resolves the database path via `gmv_core`'s own `load_config()`/`GMVPaths` (previously built in Sprint 001, unused until now) and opens the connection; callers retain `sqlite3.Connection`'s own context-manager semantics (commit/rollback on exit, connection not explicitly closed) — verified directly, not assumed. `object_service.py`'s `import sqlite3`, its hardcoded `DB` path constant, and its own `connect()` function are removed; its three call sites now use `database.connect()`. Confirmed byte-identical default path resolution against the old hardcoded `Path.home()/".gmv_core/09_DATABASE/GMV.db"` in every currently-exercised code path (production shell, both test-fixture styles) — the only new capability is that `object_service.py` now also honors `GMV_HOME` if explicitly set, inert today since nothing sets it outside test fixtures. Added: 5 focused tests for `gmv_core/database.py` (path resolution under an injected home, a live query round-trip, default-precedence resolution against the `isolated_gmv` fixture, explicit preservation of the non-closing context-manager semantics, and `ConfigurationError` propagation when neither `GMV_HOME` nor `HOME` resolves) plus one static boundary test (`tests/objects/test_object_service_boundary.py`) asserting the literal string `"sqlite3"` does not appear anywhere in `object_service.py` — deliberately scoped to this one file only, not the deferred repo-wide `ARC-002` enforcement check. All four existing `object_*` characterization tests and the OID-validation-before-database-access test pass unchanged. Live read-only sanity check against the real database (`gmv object list`/`count`/`show`) confirmed output identical to the prior baseline. Full test suite: **151 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/database.py`, `10_API/object_service.py`, `tests/test_database.py`, `tests/objects/test_object_service_boundary.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — third bounded task: migrate `10_API/service_service.py` behind the Core persistence boundary, the same repository-plus-connection-ownership pattern used for `object_service.py`, since step 1 of the roadmap's execution sequence is an incremental, one-service-per-task process per `ADR_CORE_PERSISTENCE_BOUNDARY.md`. Proposed (goal/files/acceptance criteria/human decision, no file changes) before implementation; Project Owner authorized `service_service.py` specifically as the next slice. | Complete. New `gmv_core/repositories/services.py` (`list_services`, `list_runs`, `get_service`, typed functions over an open `sqlite3.Connection`, matching the `objects.py`/`identity.py` convention) — `list_runs` preserves the existing `service_runs`/`engine_runs` UNION ALL query verbatim, including the `COALESCE`/`NOT EXISTS` de-duplication logic, unchanged. `10_API/service_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()` (the already-existing module, no changes needed). Before editing, exact current CLI output was captured by running the unmigrated script against the characterization schema fixture as ground truth (`list`/`runs`/`show`/missing-OID); after migration, the same invocations were re-run and confirmed byte-identical, avoiding hand-derivation of the UNION ALL query's result order. Added: 4 repository unit tests (`tests/services/test_service_repository.py` — renamed from an initial `test_repository.py` to avoid a pytest module-basename collision with the pre-existing `tests/objects/test_repository.py`, since this repo's test tree has no `__init__.py` packages), 1 static boundary test (`tests/services/test_service_service_boundary.py`, same scoped `"sqlite3" not in source` check as `object_service.py`'s), and 4 new characterization tests locking in exact `list`/`runs`/`show`/missing-OID CLI output. No schema change. No other `10_API` service migrated (16 of the remaining 17 direct-`sqlite3.connect` call sites are untouched). Live read-only sanity check against the real database confirmed working, correctly formatted output. Full test suite: **160 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/services.py`, `10_API/service_service.py`, `tests/services/test_service_repository.py`, `tests/services/test_service_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — fourth bounded task: migrate `10_API/search_service.py` behind the Core persistence boundary, same pattern as the two prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `search_service.py` selected over `timeline_service.py`/`event_service.py` (flagged for future merge under `MAIN-003`), `snapshot_service.py` (already "legacy" per `SNAPSHOT_BACKUP_CONTRACT.md`), and `base_service.py` (unreferenced dead code) — smallest stable, wired, zero-write, single-`connect()` candidate remaining. Project Owner authorized `search_service.py` specifically. | Complete. New `gmv_core/repositories/search.py` (`search(connection, query)`, typed function over an open `sqlite3.Connection`) preserves all four cross-table sub-queries (`objects`, `resources`, `events`, `relations`) verbatim, including the `CASE`/`instr()`/`COALESCE` matching logic and per-table `ORDER BY`. `10_API/search_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its one function now uses `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture across four cases (name/filename match, event-description match, relation-type match, no match) plus the missing-query-argument case, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/search/test_search_repository.py`), 1 static boundary test (`tests/search/test_search_service_boundary.py`, same scoped `"sqlite3" not in source` check), and 5 new characterization tests locking in the four match-category outputs plus the missing-argument usage/exit-code contract. No schema change. No other `10_API` service migrated (15 of the remaining direct-`sqlite3.connect` call sites are untouched). Live read-only sanity check against the real database confirmed correct output. Full test suite: **170 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/search.py`, `10_API/search_service.py`, `tests/search/test_search_repository.py`, `tests/search/test_search_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — fifth bounded task: migrate `10_API/resource_service.py` behind the Core persistence boundary, same pattern as the three prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `resource_service.py` selected as the cleanest remaining candidate — excluded `artifact_audit.py` (already parameterized on `Path`, imported by `doctor_service.py`, different/greater coupling), `event_service.py`/`timeline_service.py` (flagged for merge under `MAIN-003`), `snapshot_service.py` (legacy), `base_service.py`/`plugin_manager.py` (dead code), `plugin_service.py` (part of `MAIN-005` Plugin-duplication retirement), and `backup_service.py`/`status_service.py`/`gmv_compatibility.py`/`import_service.py` (already complex or write-bearing). Project Owner authorized `resource_service.py` specifically. | Complete. New `gmv_core/repositories/resources.py` (`list_resources`, `count_resources`, `get_resource`, typed functions over an open `sqlite3.Connection`, matching the established convention) preserves the `resource_view`/`resources` queries verbatim. `10_API/resource_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture (`list`/`count`/`show`/missing-OID) before editing, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/resources/test_resource_repository.py`), 1 static boundary test (`tests/resources/test_resource_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`count`/`show`/missing-OID CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct output. Full test suite: **179 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/resources.py`, `10_API/resource_service.py`, `tests/resources/test_resource_repository.py`, `tests/resources/test_resource_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — sixth bounded task: migrate `10_API/relation_service.py` behind the Core persistence boundary, same pattern as the four prior slices. Proposed (motivation/files/acceptance criteria, no file changes): `relation_service.py` selected as the last remaining candidate in the same clean list/count/show shape as the four already-migrated services, with no additional coupling — `queue_service.py` noted as a valid alternative but already partially coupled to `gmv_core.errors`/`gmv_core.validation`; `doctor_service.py` excluded for centrality (consumed by `health_service.py`). Project Owner authorized `relation_service.py` specifically. | Complete. New `gmv_core/repositories/relations.py` (`list_relations`, `count_relations`, `get_relations`, typed functions over an open `sqlite3.Connection`, matching the established convention) preserves the `relation_view`/`relations` queries verbatim, including the `source_oid=? OR target_oid=?` matching logic. `10_API/relation_service.py`'s `import sqlite3`, hardcoded `DB` constant, and its own `connect()` are removed; its three functions now use `gmv_core.database.connect()`. Ground-truth CLI output was captured from the unmigrated script against the characterization fixture (`list`/`count`/`show` as source, `show` as target, missing-OID) before editing, then re-verified byte-identical after migration. Added: 4 repository unit tests (`tests/relations/test_relation_repository.py`), 1 static boundary test (`tests/relations/test_relation_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`count`/`show`/no-match CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct output. Full test suite: **188 passed**, 0 failed (`ruff`, `git diff --check` clean on changed files). | `gmv_core/repositories/relations.py`, `10_API/relation_service.py`, `tests/relations/test_relation_repository.py`, `tests/relations/test_relation_service_boundary.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — next service selection and remaining Sprint 003 execution-sequence steps not yet authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — seventh and final simple-candidate task: migrate `10_API/queue_service.py` behind the Core persistence boundary. Preceded by a full classification of all 13 remaining direct-`sqlite3.connect` callers against the read-only/homogeneous/low-risk criteria used across this series (exclude write-bearing, legacy/dead-code, `MAIN-003`/`MAIN-005`-bound, central/strongly-coupled services); only `queue_service.py` qualified — no batch of up to 4 could be honestly composed. Project Owner authorized `queue_service.py` as the last simple slice of this phase. | Complete. New `gmv_core/repositories/queue.py` (`list_queue`, `get_queue_entry`, typed functions over an open `sqlite3.Connection`) preserves the `import_queue_view` query and the `pending_only` dynamic-`WHERE` construction verbatim. `10_API/queue_service.py`'s `import sqlite3`, hardcoded `DB` constant, its own `connect()`, and the now-unused `QUEUE_COLUMNS` constant are removed (moved into the repository module); its two functions now use `gmv_core.database.connect()`. The `validate_positive_id(import_id)` call in `show_queue_entry()` still runs before `database.connect()` — confirmed unchanged against the pre-existing `tests/cli/test_cli_validation.py::test_queue_show_rejects_invalid_id_before_database_access`. Ground-truth CLI output was captured from the unmigrated script (`list`/`pending`/`show`/missing-ID/invalid-ID/no-args) before editing, then re-verified byte-identical after migration. A pre-existing `pyproject.toml` `per-file-ignores` entry suppressing ruff `S608` (f-string SQL construction, a false positive against the static `QUEUE_COLUMNS` constant) was relocated from `10_API/queue_service.py` to `gmv_core/repositories/queue.py`, following the code it was scoped to — this is a direct, minimal consequence of the migration, not a scope expansion. Added: 4 repository unit tests (`tests/queue/test_queue_repository.py`), 1 static boundary test (`tests/queue/test_queue_service_boundary.py`), and 4 new characterization tests locking in exact `list`/`pending`/`show`/missing-ID CLI output. No schema change. No other `10_API` service migrated. Live read-only sanity check against the real database confirmed correct (empty) output, matching the real `import_queue` table's current empty state. Full test suite: **197 passed**, 0 failed (`ruff`, `git diff --check` clean on changed and full-repo files). **This closes the simple-candidate migration series** — the next roadmap gate is Sprint 003 execution-sequence step 2 (`MAIN-011`, JSON/Object identity reconciliation), not started, not scoped. | `gmv_core/repositories/queue.py`, `10_API/queue_service.py`, `tests/queue/test_queue_repository.py`, `tests/queue/test_queue_service_boundary.py`, `tests/characterization/test_cli.py`, `pyproject.toml`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — Sprint 003 execution-sequence step 2 (`MAIN-011`) is the next gate; not proposed, scoped, or authorized |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` verification and preliminary audit (read-only). Verified whether `MAIN-011` was cleanly authorizable as the Sprint 003 execution-sequence's step 2: its declared `Dependencies` (`ARC-004`, `ARC-005`, `DB-020`) in `GMV_V2_BACKLOG.md` contain two circular references — `ARC-005`'s own `Dependencies` include `MAIN-011`; `DB-020`'s own `Dependencies` include `ARC-009`, whose `Dependencies` include `DB-020` — meaning `MAIN-011` cannot be fully closed under its own declared graph without first resolving both cycles. Reported to the Project Owner rather than resolved unilaterally, with the concrete divergence found in this repository (`03_STATE/objects/OBJECT-0000001_GMV.json` vs. SQLite `PER-000001`) cited as evidence the problem is real, not hypothetical. | Project Owner ruled the two cycles block migration/closure only, not evidence collection, and authorized a bounded preliminary audit slice. Every identifier under `03_STATE/objects/*.json` (1 file found: `OBJECT-0000001_GMV.json`) and `02_INDEXES/OBJECT_INDEX.json` (1 declared entry, consistent with the directory, no orphans) was cross-referenced against all 12 rows of the SQLite `objects` table by exact-OID match and by `type`+`name` match, and checked against `OID_CONTRACT.md`'s grammar. Result: `OBJECT-0000001` is format-invalid (`OBJECT` is 6 letters where the contract requires exactly 3; `0000001` is 7 digits where it requires exactly 6) and matches `PER-000001` by `type`/`name` (`Person`, `Giacomo Marco Valerio`) — classified **CONFLICT**: a non-conformant identifier aliasing an existing canonical identity for the same entity. Recorded in new `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md` (Scope/Method/Findings/Mapping Table/Completeness Statement/What This Audit Does Not Resolve sections). No JSON, database, or code was modified. No ADR was written; no architecture was proposed — both explicitly out of scope for this slice. | `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Project Owner decision required: (1) which identifier is authoritative, `OBJECT-0000001` or `PER-000001`; (2) how to sequence `MAIN-011`'s migration around the `ARC-005`↔`MAIN-011` and `DB-020`↔`ARC-009` cycles — same category of decision as `ADR_CORE_PERSISTENCE_BOUNDARY.md`, not yet made |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` canonical-identity migration slice. Project Owner ruled `PER-000001` canonical, `OBJECT-0000001` a documented legacy alias, and authorized migration of only the two already-mapped artifacts, independent of `MAIN-011`'s closing dependencies. Instructed: first search all tracked files for live references to `OBJECT-0000001` outside the two mapped artifacts and stop if any exist; otherwise record the decision in an ADR and migrate. | `git grep -n "OBJECT-0000001"` across all tracked files found matches only in `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md`, `00_CONFIG/PROJECT_STATUS.md` (this ledger's own prior entries), and pre-existing prose in `GMV_TECHNICAL_REVIEW.md`/`GMV_V2_BACKLOG.md`/`GMV_V2_EXECUTION_ROADMAP.md` describing the same known conflict — no code, no other data file. A further check for path-based (not string-literal) consumers of `03_STATE/objects/` or `02_INDEXES/` in `*.py`/`*.sh`/`11_CLI/gmv` found one incidental match, `scripts/check_runtime_git_policy.py`'s generic protected-directory list — unrelated to the identifier itself. No live references found; proceeded. Recorded the decision in new `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md` (Context/Decision/Consequences/References). Migrated `03_STATE/objects/OBJECT-0000001_GMV.json` → `03_STATE/objects/PER-000001_GMV.json` (only the `oid` field changed, all other structure/content byte-for-byte identical) and `02_INDEXES/OBJECT_INDEX.json` (`oid` and `path` updated, `version`/`created_at`/`type`/`name`/`status` unchanged). Both directories are gitignored per `RUNTIME_DATA_GIT_POLICY.md` (`.gitignore` lines 12–13) — the migration is a real filesystem change, confirmed by targeted verification (valid JSON, correct `oid` in both files, index path resolves to the renamed file and the file exists, no remaining `OBJECT-0000001` string in either file, all non-identifier fields preserved, old filename no longer exists), but is not itself trackable in git; only the ADR and this ledger entry are part of the commit. Full test suite: **197 passed**, unchanged (no tracked code touched). `ruff` and `git diff --check` clean. | `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`, `00_CONFIG/PROJECT_STATUS.md` (untracked filesystem migration: `03_STATE/objects/PER-000001_GMV.json`, `02_INDEXES/OBJECT_INDEX.json`) | this commit (see `git log`) | None for this slice — `MAIN-011`'s remaining scope (parallel-write enforcement, `ARC-005` reconciliation) still requires future authorization and remains blocked on the two circular backlog dependencies |
| 2026-07-18 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` diagnostic enforcement slice: add a tracked `gmv doctor --strict` check over `03_STATE/objects/*.json`/`02_INDEXES/OBJECT_INDEX.json`. Project Owner authorized a check that fails on non-conformant OID, file/index inconsistency, or `type`+`name` duplication against a differently-numbered SQLite OID, and passes when the runtime files are absent — reusing existing `gmv_core` validators/paths, no writes, no schema change. | Complete. New `gmv_core/json_identity_audit.py` (`audit_json_identities(paths, connection) -> IdentityAuditResult`) implements the three failure categories, reusing `gmv_core.identity.validate_oid` (including its `expected_type` parameter, so a JSON record's declared `type` is also checked against its OID's prefix) and new `gmv_core.paths.GMVPaths` properties (`state`, `state_objects`, `indexes`, `object_index`) added following the existing property pattern. `10_API/doctor_service.py`'s `run_checks()` gained an optional `home: Path \| None = None` parameter (default preserves the prior single-argument call site in `health_service.py` unchanged) and now appends an `identity.json_conformance` check via the existing `_with_database` helper — a nonexistent database (already covered by `test_missing_database_reports_all_dependent_failures`) still fails this check too, since it can't open the required read-only connection, preserving that test's "all checks fail together" assumption without modification. Explicit module and CLI docstring note: this is **diagnostic enforcement only** — it detects and reports a conflict after the fact via `gmv doctor --strict`; it does **not** lock, freeze, or physically prevent a write to either file. `MAIN-011`'s "reject new parallel state writes" requirement (physical prevention) is a distinct, still-unimplemented, unauthorized requirement. Added: 5 new tests in `tests/identity/test_json_identity_audit.py` (absence-PASS, conformant-PASS, and one FAIL test per error category), 2 new path-property assertions in `tests/test_foundation.py`, and updated `tests/cli/test_strict_doctor.py`'s exact-check-list assertion (the only existing test affected — verified by running the full existing suite before writing this test's fix, confirming the other three `test_strict_doctor.py` tests already passed unmodified). Live check against the real repository confirmed `PASS`, validating the previously-migrated `PER-000001_GMV.json` end-to-end. Full test suite: **202 passed**, 0 failed (`ruff`, `git diff --check` clean). | `gmv_core/paths.py`, `gmv_core/json_identity_audit.py`, `10_API/doctor_service.py`, `tests/identity/test_json_identity_audit.py`, `tests/test_foundation.py`, `tests/cli/test_strict_doctor.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — `MAIN-011`'s physical write-prevention requirement and the broader `ARC-005` reconciliation remain future, separately-authorized work |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `MAIN-011` governed-freeze declaration. Instructed to verify writer existence across the whole `~/.gmv_core` tree (tracked, untracked, gitignored) and every `com.gmv.*` LaunchAgent target script, without archaeology outside `~/.gmv_core` and its LaunchAgents, and to record findings as a governance addendum — no code, no new architecture, `ARC-005`/`DB-020`/`ARC-009` not to be declared resolved. | Complete. Exhaustive `grep` across tracked/untracked/gitignored files (including `.gmv_runtime/`, `99_SYSTEM/`, `bin/`, `env/`, `00_CONFIG/lce/`) and `launchctl print`/`print-disabled` checks on every `com.gmv.*` agent's target script found zero writers to `03_STATE/objects/*.json` or `02_INDEXES/OBJECT_INDEX.json` — only the read-only diagnostic check from the prior slice and prose documentation. `com.gmv.morningbrief.email`'s target (`~/GMV_CORE/06_RUNNER/morning_os.py`) was checked as a LaunchAgent but not traversed further, per the explicit external-root exclusion; it is disabled regardless. Recorded as an addendum to `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md` (dated 2026-07-19): both artifacts declared runtime-derived and non-authoritative; no writer authorized; any future writer requires explicit Project Owner approval and must route through `gmv_core.identity`/`gmv_core.repositories.identity`. The addendum explicitly separates three concepts that must not be conflated — governed freeze (this declaration, policy-level, no enforcing code), diagnostic enforcement (the existing `identity.json_conformance` check, after-the-fact only), and filesystem immutability (does not exist — no technical control blocks a write). `ARC-005`, `DB-020`, `ARC-009` explicitly not declared resolved. No code added. | `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — `MAIN-011`'s physical write-prevention mechanism, if ever authorized, and the `ARC-005`/`DB-020`/`ARC-009` cycles remain future, separately-authorized work |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `AUTO-003` first slice (test-only). Preceded by an integrated read-only analysis (schema field correspondence, live row counts/overlap/divergence, exact writer payload semantics, `AUTO-001` status, existing migrations/tests, deterministic-fixture technique, safe-rollback shape) confirming `timeline` and `events` share identical schema, that `timeline_view` already reads from `events` (not raw `timeline`), and correcting `DB-005`'s own stale divergence count (documented as 2, actually 26+ and growing daily). Project Owner authorized test-only scope: reconciliation tests over deterministic fixtures (identical rows, same-id/divergent-content rows, events-only, timeline-only, historical writer) plus a static writer-boundary test allow-listing exactly the two known active writers. | Complete. `tests/characterization/test_timeline_events_reconciliation.py` (4 tests) and `tests/characterization/test_timeline_writer_boundary.py` (1 test) added, built on `SCHEMA_FIXTURE`/`characterized_database` (never the live database, never wall-clock dates or current live counts). Comparison logic operates on full row content keyed by id, not id presence alone — directly exercising the fixture's own pre-existing id=1 same-id/divergent-content row plus four new deterministic cases. The boundary test uses `git grep -l "INSERT INTO timeline" -- '*.py'` (excluding `tests/`) and asserts the result equals exactly `{01_RUNTIME/knowledge_engine.py, 10_API/gmv_compatibility.py}` — the same two writers identified in the prior analysis, confirmed as the only tracked direct writers to legacy `timeline` today. No production code, schema, or data modified (empty tracked `git status` diff prior to staging these two new files). Full test suite: **207 passed**, 0 failed (`ruff`, `git diff --check` clean). `AUTO-003` is **partial**: only its test/fixture/CI-detection portion is done; `DB-005`'s actual migration (switch both writers to Events, reconcile and migrate the divergent rows, verify parity, then drop `timeline`) has not begun and is not authorized by this task. | `tests/characterization/test_timeline_events_reconciliation.py`, `tests/characterization/test_timeline_writer_boundary.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — `DB-005`'s migration work (writer switch, row reconciliation, parity verification, table drop) requires its own future authorization; not started |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` first writer slice. Preceded by a read-only comparison of the two remaining `timeline` writers (scheduler dependency, payload semantics, transaction, blast radius); Project Owner authorized migrating the lower-risk `01_RUNTIME/knowledge_engine.py` first — single active scheduler (`com.gmv.knowledge`), static payload, isolated script — deferring `10_API/gmv_compatibility.py` (shared by multiple active schedulers, dynamic payload, central compatibility layer) as riskier and more complex. Authorized scope: replace only the Timeline write with the equivalent Events write, no dual-write, no historical migration, no schema/scheduler/other-writer change. | Complete. `01_RUNTIME/knowledge_engine.py`: single-line change, `INSERT INTO timeline` → `INSERT INTO events`, identical payload/value-order/transaction (still one `conn.commit()` shared with `objects`/`engine_runs`). The pre-existing `CREATE TABLE IF NOT EXISTS timeline` bootstrap was deliberately left untouched (now unused by this script, but not a schema change and not functionally significant since the table already exists in production) — noted as a minor, documented nuance rather than silently removed. New `tests/characterization/test_knowledge_engine_writes_events.py` (3 tests): subprocess invocation of the real script against a disposable `.gmv_core` home under `cli_environment`/`characterized_database` (never the live database — the writer was not run manually against it at any point), confirming a new, complete `events` row with the exact expected fields, zero new `timeline` rows, and unchanged `objects`/`engine_runs` behavior including `INSERT OR IGNORE` idempotency verified across two consecutive runs. `tests/characterization/test_timeline_writer_boundary.py`'s allow-list reduced from two writers to one (`10_API/gmv_compatibility.py` only) — re-run and confirmed passing. Full test suite: **210 passed** (3 new), 0 failed (`ruff`, `git diff --check` clean). `DB-005`'s remaining scope (second writer, row reconciliation, parity verification, table drop) not started and not authorized by this task. | `01_RUNTIME/knowledge_engine.py`, `tests/characterization/test_knowledge_engine_writes_events.py`, `tests/characterization/test_timeline_writer_boundary.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — migrating `gmv_compatibility.py`, reconciling divergent rows, verifying parity, and dropping `timeline` each require their own future authorization |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` second writer slice. Preceded by a read-only flow reconstruction of `10_API/gmv_compatibility.py` (subprocess execution, hash-pin verification, `engine_runs`/`timeline` writes, `operations.jsonl` audit, transaction, error handling), confirming exclusive Timeline→Events substitution preserves atomicity, output, exit code, and audit, and surfacing a new risk absent from the first writer: `init_db()`'s bootstrap creates `timeline` but never `events`, and this file's three existing test files (`test_command_argv.py`, `test_process_bounds.py`, `test_release_pins.py`) all rely on a fresh, unfixtured database — a same-substitution-only change would have broken all of them. Project Owner authorized replacing the `timeline` bootstrap with an `events` bootstrap (not keeping both), then the same INSERT-target substitution as the first writer. | Complete. `10_API/gmv_compatibility.py`: two-line change — `CREATE TABLE IF NOT EXISTS timeline` → `CREATE TABLE IF NOT EXISTS events` (same schema) in `init_db()`, and `INSERT INTO timeline` → `INSERT INTO events` in `run_engine()`, identical payload/value-order, still the single shared transaction with `engine_runs`. `timeline` is no longer created or written by this script at all. New `tests/compatibility/test_events_migration.py` (4 tests, matching the file's existing fresh-database convention rather than `characterized_database`): correct `events` row, `timeline` absent from `sqlite_master` entirely on a new database, `engine_runs`/`operations.jsonl` audit content unchanged, and both `engine_runs`/`events` at count 1 confirming the shared transaction. All 14 pre-existing compatibility tests re-run and confirmed green, unmodified (none referenced `timeline`). `tests/characterization/test_timeline_writer_boundary.py` converted from a shrinking temporary allow-list to a permanent, empty-set regression guard — both known writers are now migrated, so no tracked file may write directly to `timeline` going forward. Never run against the live database. Full test suite: **214 passed** (4 new), 0 failed (`ruff` including full-repo, `git diff --check` clean). `DB-005`'s writer migration is complete; the table itself, its historical rows, and dropping it are unchanged and not authorized by this task. | `10_API/gmv_compatibility.py`, `tests/compatibility/test_events_migration.py`, `tests/characterization/test_timeline_writer_boundary.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None — reconciling the historical `timeline` rows, verifying parity against `events`, and dropping the `timeline` table each require their own future authorization |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` historical-row migration tool (isolated build and test only). Preceded by two read-only design passes: (1) canonical-convention research (`gmv_core/migrations.py`/`migration_sql/*.sql` found scoped to schema DDL via `PRAGMA user_version`, not applicable; `backup_service.py`'s manifest + `audit_integrity.py`'s hash chain, plus `RUNTIME_DATA_GIT_POLICY.md`'s gitignored-runtime-evidence convention, identified as the correct precedent — evidence belongs in `04_LOGS/`, hash-chained, reusing the existing `audit_integrity` module) and (2) an explicit crash-window risk analysis (COMMIT-then-log, matching `create_backup()`'s own ordering, justified because `timeline`'s permanence makes content-based reconstruction always possible). Project Owner then authorized full isolated implementation with one-to-one deterministic multiset matching (an Events row may reconcile at most one Timeline row) and fail-closed `apply` preconditions. | Complete. `gmv_core/timeline_events_migration.py` (`plan_migration`, `apply_migration` — requires an active transaction, matching `gmv_core.repositories.identity`'s own contract — and `reconcile_evidence`) and `10_API/timeline_events_migration_service.py` (`plan`/`apply`/`reconcile` CLI) added exactly as designed: `apply` fails closed without `--confirm`, without `--backup`, with a `kind="rolling"` (non-milestone) backup, or with a backup that fails `verify_backup()` — before any writable connection is opened. `apply` migrates inside one `BEGIN IMMEDIATE` transaction, then appends hash-chained evidence to `04_LOGS/timeline_events_migration.v1.jsonl` only after the transaction has already committed. `reconcile` reconstructs the true current mapping from `timeline`/`events` content alone and backfills only whatever is missing from the existing (possibly absent or partial) evidence log, never duplicating an already-logged pair, and fails closed if the existing log's hash chain is tampered. 21 new isolated tests: 9 in `test_migration_module.py` (including the required duplicate-identical-content multiset case and two direct crash-simulation reconstruction cases), 12 in `test_migration_service_cli.py` (including every `apply` fail-closed precondition, successful migration with evidence-after-commit ordering, idempotent re-`apply`, a full crash-recovery-via-`reconcile` scenario, and a partial-log gap-only backfill without duplication). No backup was created against the live system; no migration, `apply`, or `reconcile` was ever run against `09_DATABASE/GMV.db`; no `DROP`; no scheduler touched — confirmed by the live `timeline`/`events` counts (31/9) being unchanged before and after, and by all three new paths being entirely new, untracked files with zero pre-existing tracked file modified. Full test suite: **235 passed** (21 new), 0 failed (`ruff` including full-repo, `git diff --check` clean). | `gmv_core/timeline_events_migration.py`, `10_API/timeline_events_migration_service.py`, `tests/timeline_events_migration/` (2 files), `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Live execution — an explicit, separate Project Owner authorization to create a real milestone backup and run `apply` against the live database — has not been sought or granted by this task |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` **live** Timeline→Events migration execution, Project Owner-authorized in three explicit steps: (1) a read-only `plan` dry-run against the live database (hash/mtime confirmed unchanged before and after; 26 rows classified — 4 id-collision, 22 timeline-only — matching the prior analysis exactly, no divergence); (2) creation and independent verification of milestone backup `BKP-20260719T093238Z-3b7d5659` (`--kind milestone --milestone "DB-005-timeline-events-migration"`, `source_commit de57b60`); (3) live `apply --confirm --backup <that exact backup>` after resolving exact CLI syntax via `--help` and confirming its `--database`/`--home` defaults targeted the live path with no divergence. | Complete. Backup: `verify_backup()` passed (`integrity: ok`, `foreign_key_violations: 0`, clean `oid_continuity`, 12 objects, 0 duplicates/malformed); manifest hash `ae2f67fd7175cf61b2f8a940f79cc9d1a6b9e0f2a3aa233eced6a0d4314c307e`; database fingerprint `d081553a31b54d7813f37126aa2f2ddffa195fd30f4777e05978c9003d75bac1`. Migration: **26 rows migrated**; `timeline` unchanged at **31 rows**; `events` grew **9 → 35**; exact mapping `timeline_id` 6–31 → `events_id` 10–35 confirmed with full row content identical for every pair (not just id continuity). Post-migration: `plan` reports zero rows pending; `PRAGMA integrity_check = ok`; `PRAGMA foreign_key_check` = 0 violations; evidence log `04_LOGS/timeline_events_migration.v1.jsonl` hash-chain valid, 26 contiguous records (sequence 1–26), all `action="timeline_row.migrated"`, 26 unique `(timeline_id, events_id)` pairs; `git grep -l "INSERT INTO timeline"` (tracked, excluding `tests/`) returns nothing — no writer directly targets `timeline` anywhere in the codebase. Database hash before `891e5de8c2d4595717e54a3fc436c3831d97f3c5db0fa0c5fda42913b1753ae0`, after `b5bde1a6dcd8484b43b1182c25ed99da64fbc9f9d6fe3c6930fe00eb9096b96a` — the single expected divergence, matching exactly the authorized write; `git status` clean throughout, no tracked file touched. Neither `reconcile` nor `restore` nor `DROP TABLE` was run at any point. `timeline` itself remains fully intact, unaltered, and un-dropped. | (runtime-only, gitignored: `09_DATABASE/GMV.db`, `04_LOGS/timeline_events_migration.v1.jsonl`, `~/.gmv_backups/sets/BKP-20260719T093238Z-3b7d5659/`); `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | Dropping `timeline` (or reducing it to a view over `events`) and the pre-migration backup's own isolated restore-check remain outstanding against `GMV_V2_EXECUTION_ROADMAP.md`'s Sprint 003 exit criteria — neither authorized by this task |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — live schema-version promotion `0→2`, explicitly Project Owner-authorized after a read-only gate found that migration implementations 001/002 existed but the live database and the DB-005 milestone backup both remained formally unversioned (`PRAGMA user_version = 0`), with no `oid_sequences` table. The same analysis proved the live schema signature exactly matched migration 001's baseline, so adoption was deterministic; it also confirmed the separately executed DB-005 milestone restore-check as a valid `restore.check` audit record (sequence 1236, outcome `verified`), correcting the prior entry's now-stale pending statement. Authorized scope: create/verify one new post-data-migration milestone backup; run only the canonical migration runner to target v2; verify; update this ledger and commit. No migration 003 or Timeline schema conversion. | Complete. Created verified milestone `BKP-20260719T162205Z-881dbdd4` (`source_commit 4144052`, milestone `DB-005-post-data-migration-pre-schema-v2`): 35 Events, 12 Objects, clean OID continuity, `integrity: ok`, zero FK violations, backup DB at `user_version=0`; hash-chain-valid `backup.create` sequence 1263. Ran `python -m gmv_core.migrations --database <live> --target-version 2`, which adopted baseline v1 then applied v2. Live result: `user_version=2`; `oid_sequences` seeded `COR=1`, `PER=1`, `PLG=4`, `RES=1`, `SRV=4`, `SYS=1`; integrity OK; zero FK violations; all pre-existing table rows are identical to the backup; Timeline→Events unmatched count zero; `timeline` still a table at 31 rows and `events` still 35. Database hash `b5bde1a6...` → `99ee2714...`. Full suite **235 passed**; scoped migration/Core lint passes. Repository-wide Ruff reports only three pre-existing out-of-scope findings (tracked frozen Apprentice `F541` ×2; untracked `99_SYSTEM` `S603` ×1), left untouched. | Runtime-only/gitignored: `09_DATABASE/GMV.db`, `~/.gmv_backups/sets/BKP-20260719T162205Z-881dbdd4/`; tracked: `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — the next DB-005 gate is a separate migration 003 task, isolated/tested first, requiring parity verification and Timeline table→view replacement to be atomic in one `BEGIN IMMEDIATE` transaction |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` migration 003 isolated implementation. Per the standing destructive-operation rule, Project Owner explicitly authorized `DROP TABLE timeline` only inside disposable test databases; live DB, backup, restore, scheduler, and audit were excluded. Scope derived directly from the prior dashboard gate, roadmap step 3/exit criterion, and backlog DB-005: implement the smallest versioned schema slice that atomically verifies legacy parity and substitutes the table with an Events-backed compatibility view. | Complete. Added `gmv_core/migration_sql/003_timeline_view.sql` with a single `BEGIN IMMEDIATE` transaction: a temporary CHECK guard rejects any full-content group whose Timeline multiplicity exceeds Events; only after that check does the migration drop the table, create the six-column `timeline` view over `events`, set `user_version=3`, and commit. Any parity failure or post-DROP SQL error rolls everything back. Extended `gmv_core.migrations` to support explicit target v3 and chained v1→v2→v3 execution, while deliberately retaining `CURRENT_SCHEMA_VERSION=2` so current production writers remain compatible until the live cutover. Added four tests covering explicit/non-default activation plus repeat byte stability, successful duplicate multiset parity with Events-only visibility, fail-closed deficit, and rollback after a simulated post-DROP failure. Full suite **239 passed**; scoped Ruff and `git diff --check` pass. Live DB proved untouched: hash `99ee2714...`, `user_version=2`, Timeline table 31, Events 35, zero unmatched. | `gmv_core/migration_sql/003_timeline_view.sql`, `gmv_core/migrations.py`, `tests/migrations/test_timeline_view.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — live v3 cutover requires separate explicit Project Owner approval because it includes backup and live DROP; until then v2 remains current and migration 003 is explicit-target only |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-005` live migration 003 cutover. Project Owner explicitly authorized one bounded slice containing milestone backup creation/verification, promotion of `CURRENT_SCHEMA_VERSION` to 3, live execution of migration 003 including `DROP TABLE timeline` and Events-backed view creation, verification, ledger update, and one commit; restore and scheduler operations were explicitly excluded. | Complete. Verified preconditions at HEAD `9df89bf`: live v2 hash `99ee2714...`, Timeline table 31, Events 35, zero multiset deficit, no tracked Timeline writer. Created verified milestone `BKP-20260719T164115Z-ffe0e01a` (`source_commit 9df89bf`, audit sequence 1266), then ran explicit target v3. Live is now hash `c3eb35d8...`, `user_version=3`, Timeline a six-column read-only view equal to all 35 Events; legacy multiset fully preserved; all other tables identical to backup; integrity and FK checks pass; OID sequences unchanged. Set `CURRENT_SCHEMA_VERSION=3`; updated importer failure-contract expectation. Full suite **239 passed**; scoped Ruff and diff checks pass. Database/identity/permissions and backup health checks pass; strict Doctor/Status retain only documented pre-existing failures outside scope (missing run-2 stdout/stderr artifacts, stale service freshness, DB-002 enforcement unavailable). No restore, scheduler change, audit redesign, ADR, or unrelated remediation. `DB-005` complete. | Runtime-only/gitignored: `09_DATABASE/GMV.db`, `~/.gmv_backups/sets/BKP-20260719T164115Z-ffe0e01a/`; tracked: `gmv_core/migrations.py`, `tests/migrations/test_timeline_view.py`, `tests/identity/test_allocation.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical roadmap item is `DB-004`; compensating Event correction/supersession semantics require Project Owner decision before enforcement design |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-004` append-only semantics decision and isolated migration 004. Project Owner approved the recommended domain rule: Events are immutable; a correction is a new Event with an explicit reference to the Event it supersedes. Scope was bounded to ADR, explicit migration target, disposable-database tests, ledger, and one commit; live DB, backup, restore, scheduler, and operational audit were excluded. | Complete. Added accepted `00_CONFIG/ADR_DB004_APPEND_ONLY_EVENTS.md`. Added transactional `004_append_only_events.sql`: nullable `supersedes_event_id`; database triggers rejecting update, delete, existing-ID reuse/replace, and nonexistent supersession targets; version advances to 4 only on commit. Runner accepts explicit v4 but keeps current/live version 3. Five isolated tests cover v4 explicitness/idempotence, correction chain insertion, Timeline visibility, invalid reference, update/delete/replace rejection, and failure rollback. Full suite **244 passed**; scoped Ruff and diff checks pass. Live proved untouched: hash `c3eb35d8...`, v3, Timeline view, six-column Events, zero Event triggers. | `00_CONFIG/ADR_DB004_APPEND_ONLY_EVENTS.md`, `gmv_core/migration_sql/004_append_only_events.sql`, `gmv_core/migrations.py`, `tests/migrations/test_append_only_events.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — live v4 cutover requires separate explicit Project Owner approval for milestone backup and live database migration |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — `DB-004` live migration 004 cutover. Project Owner explicitly authorized one bounded slice containing milestone backup creation/verification, live migration, v4 promotion, writer/trigger/data verification, ledger, and one commit; restore and scheduler operations excluded. | Complete. Preconditions at HEAD `87d05a9`: live v3 hash `c3eb35d8...`, 35 Events, no triggers; four tracked Event writers confirmed explicit-column compatible, Timeline bootstrap against view confirmed no-op. Created verified milestone `BKP-20260719T165725Z-f484b79b` (`source_commit 87d05a9`, audit sequence 1268), then applied migration 004. Live is now hash `0ed2cc14...`, `user_version=4`, seven-column Events with 35 unchanged rows/all supersession references null, Timeline view intact, and four append-only/reference triggers installed. Promoted `CURRENT_SCHEMA_VERSION=4`. Controlled live negative tests rejected update, delete, replace, and missing reference; rollback preserved the exact database hash. Full suite **244 passed** including writer tests; scoped Ruff/diff checks pass. Operational failures remain only the documented pre-existing artifact/freshness/DB-002 items. No restore, scheduler change, audit redesign, or unrelated fix. `DB-004` complete. | Runtime-only/gitignored: `09_DATABASE/GMV.db`, `~/.gmv_backups/sets/BKP-20260719T165725Z-f484b79b/`; tracked: `gmv_core/migrations.py`, `tests/migrations/test_timeline_view.py`, `tests/migrations/test_append_only_events.py`, `tests/identity/test_allocation.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical roadmap item is `DB-006`/`ARC-006`/`MAIN-014`; Engine→Service mapping and reconciliation policy require Project Owner decision |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — first `DB-006`/`ARC-006`/`MAIN-014` slice. Project Owner approved the recommended reconciliation rule: migrate/deduplicate authorized runs through the four registered Service identities and preserve the single frozen `gmv_core` Constitution anomaly as evidence without granting it a Service OID. Scope deliberately limited to recording that architectural decision and moving the native Knowledge Engine writer; live data, backup, restore, DROP, scheduler, compatibility writer, historical reconciliation, and read-side merge removal excluded. | Complete. Accepted `ADR_DB006_ENGINE_SERVICE_RECONCILIATION.md` records the exact mapping and null-safe full-payload reconciliation policy. `knowledge_engine.py` now creates/writes `service_runs` with `SRV-000001` and `Knowledge Engine` in its existing shared transaction, and no longer creates or writes `engine_runs`; its normative Service contract matches. Isolated tests prove the canonical row, zero legacy-ledger growth, repeat append behavior, and full transaction rollback on a rejected Service Run. Targeted **8 passed**; full suite **245 passed**; scoped Ruff/diff checks pass. Live DB remained byte-identical at `0ed2cc14...`, v4, 31 Engine Runs/5 Service Runs. | `00_CONFIG/ADR_DB006_ENGINE_SERVICE_RECONCILIATION.md`, `01_RUNTIME/knowledge_engine.py`, `tests/characterization/test_knowledge_engine_writes_events.py`, `00_CONFIG/SERVICE_SPECIFICATION.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None formally approved — next canonical bounded slice is the Compatibility Layer writer migration using the accepted three-name mapping and fail-closed unknown-name handling |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — second `DB-006`/`ARC-006` writer slice. With the identity and anomaly policy already accepted, no new Project Owner decision was required. Scope: migrate only the Compatibility Layer writer to the three approved Service identities, preserve its operational contracts, add isolated tests and a permanent no-Engine-writer guard; no live execution/data, historical migration, backup, restore, DROP, scheduler, or read-side change. | Complete. `gmv_compatibility.py` now uses a closed mapping for `SRV-000002…004`, rejects valid-but-unregistered names before any side effect, bootstraps `service_runs` rather than `engine_runs`, and writes the full Service identity in the unchanged transaction with Events. All wrapper names already matched the approved map, so no scheduler file changed. Service specifications now declare canonical Service Runs and prohibit new Engine writes. Tests cover all mappings, fresh-schema boundary, audit/process/argv/release behavior, unknown-name failure, and zero tracked runtime INSERTs into `engine_runs`: targeted **22 passed**, full suite **249 passed**, Ruff/diff checks pass. Live DB remained byte-identical at `0ed2cc14...`, v4, 31/5 legacy/canonical rows. | `10_API/gmv_compatibility.py`, `tests/compatibility/`, `tests/characterization/test_engine_run_writer_boundary.py`, `00_CONFIG/SERVICE_SPECIFICATION.md`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None formally approved — next canonical slice is isolated historical reconciliation tooling under the accepted policy; all live/destructive gates remain separate |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — isolated `DB-006` historical reconciliation tool. Scope followed the accepted mapping/anomaly ADR and the same proven plan/apply/reconcile plus hash-chain evidence pattern used for DB-005. Disposable-database execution only; the new tool was never pointed at live. No real backup, restore, DROP, scheduler, table retirement, migration 005, or read-side merge removal. | Complete. New module performs deterministic one-to-one full-payload matching, migrates only mapped missing rows inside the caller transaction, preserves `engine_runs`, permits exactly the approved run-23 exclusion, and rejects all other unmapped/divergent states. New CLI keeps `plan` read-only; requires confirm plus verified milestone backup for apply; enforces initial 5/25/1 or final 30/0/1 cardinality; writes hash-chained migrated/excluded evidence only after commit; reconstructs crash-window gaps and rejects corrupt logs. Twenty isolated tests reproduce the real 31/5 gate and all failure/idempotence contracts. Full suite **269 passed**; Ruff/diff checks pass. Live hash remained `0ed2cc14...`. | `gmv_core/engine_service_runs_migration.py`, `10_API/engine_service_runs_migration_service.py`, `tests/engine_service_runs_migration/`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical gate is live read-only plan and requires explicit Project Owner approval; backup/apply remain later gates |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — explicit Project Owner authorization for the next canonical `DB-006` gate: run only the new reconciliation tool's read-only `plan --json` against the live database, verify exact 5/25/1 classification and prove file immutability. Backup, apply, reconcile, restore, DROP, scheduler, and every write excluded. | Complete. At HEAD `b50780a`, live pre/post SHA-256 `0ed2cc14...`, mtime `1784480263`, and size 135168 bytes are identical. Plan returned exactly 5 matched, 25 pending, 1 excluded; run 23 matched the approved frozen anomaly with content hash `14cb0faf...f66a8d`; no unmapped divergence. No evidence log was created. Migration tests **20 passed**. No code or runtime state changed. | `00_CONFIG/PROJECT_STATUS.md` only | this commit (see `git log`) | None approved — next gate is create/verify a milestone backup; live apply remains a subsequent, separate authorization |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — explicit Project Owner authorization for the `DB-006` milestone backup gate only. Preconditions: HEAD `41292a8`, live hash/mtime/size unchanged from the approved plan. Scope: create and independently verify a milestone backup, validate classification on the copy and audit chain; no live apply/reconcile, restore, DROP, scheduler, or schema/read-side change. | Complete. Created `BKP-20260719T173902Z-5e7de08d` (`DB-006-engine-service-runs-live-apply`, source commit full `41292a8...`). Verification: v4, integrity OK, zero FK violations, 35 Events, 12 Objects, clean OIDs; backup DB hash `e9eafae...32c0e8`, manifest hash `9b079f75...cf020`; backup-copy plan exactly 5/25/1. Backup audit chain valid through sequence 1272, verified record hash `b78f2f37...608d`. Live remained `0ed2cc14...`, same mtime/size; no migration evidence. Tests **20 passed**. | Runtime-only/gitignored backup plus `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next gate is live apply using this exact backup and requires new explicit authorization; restore/DROP remain excluded |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — explicit Project Owner authorization for live `DB-006 apply --confirm` using only verified backup `BKP-20260719T173902Z-5e7de08d`; verification/evidence validation included, but reconcile, restore, DROP/table retirement, scheduler, and read-side merge removal excluded. | Complete. Preflight backup verified and live plan 5/25/1. Apply inserted exactly 25 canonical Service Runs and logged run 23 as the approved frozen exclusion; final plan 30/0/1, Service distribution 19/4/6/1. Source Engine rows, Events, Objects, and five pre-existing Service rows are byte-for-row identical to backup; all 30 mappings have exact payload counterparts; exclusion absent from Service Runs. DB remains v4/integrity OK/zero FK violations, new hash `8c0a729a...1a2025`. Evidence chain valid, 26 records, hash `e0187aad...5d813`. A full-suite-only runtime policy failure revealed literal personal paths in the tracked exclusion fixture; implementation was immediately hardened to ID+full-payload-hash pinning without semantic change. Final full suite **269 passed**, Ruff/policy tests pass. | Runtime-only/gitignored live DB/evidence; tracked `gmv_core/engine_service_runs_migration.py`, two migration test files, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next gate is live reconcile to backfill five pre-existing exact mappings into evidence; DROP/read-side tasks remain later |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — explicit Project Owner authorization for live `DB-006 reconcile` only: complete evidence for the five exact Service rows that predated apply, verify DB immutability and idempotence. Apply, backup, restore, DROP/table retirement, scheduler, and read-side changes excluded. | Complete. Precondition 30/0/1; DB hash `8c0a729a...1a2025`; evidence 26 valid records. Reconcile appended exactly five records mapping Engine/Service IDs 1–5. Final chain: 31 contiguous records, 25 migrated/5 reconciled/1 excluded, 30 unique pairs, terminal hash `e4107eea...517f7`, file hash `e92938d4...87e60`. Repeat reconcile returned no gaps. DB hash/mtime/size unchanged. Tests **20 passed**. | Runtime-only/gitignored evidence plus `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical slice is non-destructive `MAIN-014` removal of heuristic run merging; Engine table DROP remains separately gated |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — non-destructive `MAIN-014` read-side cutover after canonical Service Run migration and evidence reconciliation. Scope: remove only the heuristic Engine/Service merge from the repository and characterize the canonical output; no live database access/write, backup, restore, DROP/table retirement, scheduler, schema, or runtime-data operation. | Complete. `list_runs()` now selects only `service_runs`, preserving the seven-column CLI output contract with constant `source="service"` and newest-first ordering. Removed the normalized-name join, exact-timestamp deduplication, correlated `NOT EXISTS`, and all read dependence on `engine_runs`. Repository and CLI tests retain an unmatched Engine fixture row and prove it is absent from results. Targeted **32 passed**; full suite **269 passed**; scoped Ruff passes. | `gmv_core/repositories/services.py`, `tests/services/test_service_repository.py`, `tests/characterization/test_cli.py`, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical `DB-006` step is Engine table retirement/replacement and requires explicit Project Owner authorization because it performs `DROP`/destructive schema work |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — Project Owner-authorized isolated `DB-006` Engine Run retirement implementation. Scope interpreted as the smallest safe destructive-schema slice: remove residual runtime read dependencies and implement/test explicit migration 005 only on disposable databases. Live DB, backup, restore, migration evidence, scheduler, and runtime data excluded. | Complete. Non-strict Doctor and artifact audit now use canonical `service_runs` and Service identity; strict Doctor no longer requires the legacy table. Explicit migration 005 keeps the default/current target at v4 and performs parity validation, exact hash-pinned exclusion validation, `DROP TABLE engine_runs`, and `user_version=5` within one `BEGIN IMMEDIATE`. Tests prove duplicate-sensitive full-payload parity, changed-exclusion rejection, and rollback after a post-DROP failure, leaving v4/table/data intact. Targeted **59 passed**; full suite **273 passed**; Ruff and `bash -n` pass. No live operation occurred. | `gmv_core/migrations.py`, `gmv_core/migration_sql/005_retire_engine_runs.sql`, `11_CLI/gmv`, `10_API/artifact_audit.py`, `10_API/doctor_service.py`, related migration/CLI/observability/characterization tests, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical gate is verified milestone backup plus live migration 005 and requires new explicit approval; restore and scheduler remain excluded |
| 2026-07-19 | GMV V2 Sprint 003 (Data/Database) — Project Owner-authorized single controlled live migration 005 operation, conditioned on every preflight check passing before milestone backup creation and immediate cutover. Required fail-closed behavior: if any check failed, create no backup, apply no migration, and stop. | **STOPPED AT PREFLIGHT; NO CUTOVER.** Git HEAD `7cc519c`, tracked tree clean. Live DB started and ended byte-identical: SHA-256 `8c0a729a...1a2025`, mtime `1784483134`, size 135168. Reconciliation plan passed at 30 matched/0 pending/1 excluded; the approved exclusion retained content hash `14cb0faf...f66a8d`; evidence chain passed with 31 records and file hash `e92938d4...87e60`; isolated migration tests **4 passed**. Strict Doctor then failed exactly `artifacts.references`: Service Run 2's stdout and stderr paths under `05_OUTPUT/bridge/2026_07_02_123013_daily_log.*.log` are unavailable. The canonical artifact audit returned the same two failures and exit 1. Per the explicit stop condition, no milestone backup was created, migration 005 was not invoked, `engine_runs` was not dropped, `user_version` remained v4, and no restore/scheduler/runtime-data operation occurred. | `00_CONFIG/PROJECT_STATUS.md` only | this commit (see `git log`) | None approved — diagnose or explicitly resolve the two unavailable canonical artifact references before seeking a new backup/live-migration authorization |
| 2026-07-19 | Isolated resolution of Service Run 2's two missing artifact references, explicitly authorized by the Project Owner. Required: determine cause first; recover only from verifiable sources; never reconstruct or invent content; preserve historical traceability; run Doctor, artifact audit, and the full suite; no milestone backup or migration 005. | Complete. Cause is path/location drift: Run 2 retained `05_OUTPUT/bridge/...`, but the exact timestamp-matched files existed under `05_OUTPUT/compatibility/...` and are preserved in Git commit `5634276`. Current files and historical blobs match exactly: stdout blob `dc035ce2...`, SHA-256 `adedb066...bda5`, 132 bytes; stderr blob `e69de29b...`, SHA-256 `e3b0c442...b855`, 0 bytes; both mtime `2026-07-02T12:30:13+0200`. Copied those exact bytes with metadata to the recorded bridge paths; no synthesized content and no database/path rewrite, so the 30/0/1 reconciliation and hash-chained migration evidence remain historically valid. This entry corrects the earlier operational conclusion that the files were unrecoverable without rewriting that historical exception record. Artifact audit exits 0; strict Doctor all 8 PASS/`ready`; full suite **273 passed**. Live DB remains SHA-256 `8c0a729a...1a2025`, mtime `1784483134`, size 135168, v4 with `engine_runs` intact. No backup, restore, migration, scheduler, DB, or evidence-log write. | Runtime-only/gitignored recovered artifacts plus `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — new explicit authorization required for the milestone backup and conditional live migration 005 gate |
| 2026-07-19 | Project Owner-authorized single controlled milestone-backup plus conditional live migration 005 operation. Required fail-closed sequence: all preflights green; create and verify a recoverable milestone; re-run live preflight; apply immediately only if unchanged; verify v5/table removal/Service parity/exclusion/evidence/artifacts/Doctor/tests; restore only if a failure made it necessary. | Complete. Preconditions at HEAD `0e4ba42`: v4 DB hash `8c0a729a...1a2025`, 31 Engine/30 Service Runs, integrity/FK clean, plan 30/0/1, exclusion hash `14cb0faf...f66a8d`, evidence SHA-256 `e92938d4...87e60`, artifact audit exit 0, Doctor READY, migration tests 4 passed. Created verified milestone `BKP-20260719T182804Z-6d0612f1` (`source_commit=0e4ba42...`): manifest hash `cdce4a09...756bc`, DB-copy hash `c1baec37...9b34`, repository archive hash `21a37851...f37d`, v4/integrity/FK/OID clean, backup plan 30/0/1; backup audit chain valid through sequence 1276 (`da657c3e...31f4f`). Final live preflight remained identical, then migration 005 returned `schema_version=5`. Post-cutover: no `engine_runs` schema object; 30 Service rows exactly equal to backup in both directions (0 differences), distribution 19/4/6/1, zero mapped Engine deficits, only one unmapped backup row; Events 35 and Objects 12; integrity OK/zero FK; DB hash `b1941dc3...50cc`. Exclusion/evidence unchanged (31 records; terminal `e4107eea...517f7`; file `e92938d4...87e60`). Artifact audit exit 0; Doctor 8/8 PASS/READY. Promoted default schema target to v5; one stale test-message expectation surfaced and was corrected from 4 to 5, then targeted **62 passed**, full suite **273 passed**, Ruff/diff clean. No restore or scheduler action. | Runtime-only milestone/live DB; tracked `gmv_core/migrations.py`, three migration/identity test files, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical roadmap item is `DB-002` foreign keys |
| 2026-07-19 | Project Owner-authorized isolated `DB-002` policy and implementation slice only: accept the ten restrictive relationships; implement explicit migration 006; enable reliable per-connection enforcement in pertinent application paths; prove Knowledge/Compatibility fail closed without Object authority. Live DB, milestone backup, cutover, restore, and scheduler excluded. | Complete. Accepted `ADR_DB002_RESTRICTIVE_FOREIGN_KEYS.md`. Migration 006 keeps default v5 and uses one `BEGIN IMMEDIATE` to reject any of ten orphan classes before DDL, rebuild seven tables, preserve all data/views/four append-only Event triggers/`sqlite_sequence`, run an in-transaction `foreign_key_check` guard, and set v6 only on successful commit. Isolated tests prove all ten `RESTRICT/RESTRICT` declarations, ten negative writes, explicit/idempotent targeting, orphan rejection before DDL, and full rollback after an injected post-`DROP` failure. Core-owned connections now enable/read back the PRAGMA and fail if called inside a transaction where SQLite would ignore it. The three active writers use enforcement; Import works transactionally on v5/v6, while Knowledge and Compatibility validate required System/Service Objects and fail exit 2 before writes/process execution without auto-creating authority. Exact live-shape transient clone: v6, integrity OK, zero violations, row/view/trigger/sequence equality. Live hash remained `b1941dc3...50cc`. Targeted **50 passed**, full suite **285 passed**; scoped Ruff/diff clean. Global Ruff additionally reports three unrelated pre-existing findings in tracked Apprentice and untracked `99_SYSTEM` material, left untouched. | `00_CONFIG/ADR_DB002_RESTRICTIVE_FOREIGN_KEYS.md`, `gmv_core/migration_sql/006_foreign_keys.sql`, migration/connection modules, three active writers, isolated migration/connection/writer tests, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next bounded gate is isolated completion/static coverage of remaining SQLite connection classes; milestone backup/live migration 006 remain later gates |
| 2026-07-19 | Project Owner-authorized isolated `DB-002` completion of connection enforcement: inventory/classify every residual SQLite connection (diagnostic, read-only, backup, migration, dormant/historical), require verified `foreign_keys=ON` for ordinary connections, document/test only indispensable exceptions, and add a static regression guard. Live DB, milestone backup, cutover, restore, and scheduler excluded. | Complete. New explicit-target `gmv_core.database.connect_path()` owns connection creation and closes/fails if the PRAGMA readback is not 1. Migrated every tracked production caller: Event/Timeline/Plugin readers; artifact, Doctor, and Status diagnostics; Snapshot; dormant `BaseService`/`plugin_manager`; all Backup source/target/verification/evidence/OID connections; DB-005/DB-006 plan/apply/reconcile tools; and both schema-runner targets including memory. Active writers now use the same factory. Static AST coverage proves exactly one raw `sqlite3.connect()` call, owned by the factory, and scans tracked production Python/SQL for disabled enforcement. Sole exception: migration 006 turns FK enforcement off before its atomic rebuild and restores it after commit; tests prove exact allow-list, ordering, rollback, restoration to 1, and clean `foreign_key_check`. Full suite **290 passed**, scoped Ruff/diff clean. Read-only live verification: Doctor 8/8 READY, artifact audit exit 0, factory enforcement 1, live v5; SHA-256 unchanged `b1941dc3...50cc`. No operational backup or runtime mutation. | `00_CONFIG/ADR_DB002_RESTRICTIVE_FOREIGN_KEYS.md`, `gmv_core/database.py`, `gmv_core/migrations.py`, all tracked residual SQLite callers, connection/static/migration tests, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical gate is controlled milestone backup plus conditional live migration 006; requires explicit authorization |
| 2026-07-19 | Project Owner-authorized single controlled `DB-002` operation: fail-closed preflight, create and verify a recoverable milestone, apply migration 006 to live only while all gates remain green, validate the complete post-commit contract, and restore from that exact milestone only if post-commit validation failed. Scheduler intervention excluded. | Complete. At HEAD `7203be4`, preflight live v5 was SHA-256 `b1941dc3...50cc`, mtime `1784485733`, size 135168, integrity/FK clean, with zero across all ten orphan classes; Doctor 8/8 READY, artifact audit exit 0, and 18 pertinent tests passed. Created milestone `BKP-20260719T193423Z-494bea46` (`source_commit=7203be4...`): manifest `34d7b4d8...a8f7`, database `a9e0b29c...1af61`, repository archive `1fb369e4...071d`; canonical verification returned v5/integrity OK/zero violations/clean OID continuity, the archive index was readable, and a disposable database copy migrated to v6 with 10/10 restrictive foreign keys. The backup audit chain is valid through 1284 records; this milestone's verified creation is sequence 1281, record `70810e71...d820`. Final live preflight remained byte-identical, then migration 006 returned `schema_version=6`. Live SHA-256 is `131245ce...5735`; all ten declarations are `RESTRICT/RESTRICT`, connection enforcement is 1, integrity is OK, and `foreign_key_check` is empty. Symmetric differences against the verified copy are zero for all 11 application tables, all 8 views, and `sqlite_sequence`; four Event triggers and exact sequences 12/35/4/9/30 are preserved. Doctor 8/8 READY; artifact audit exit 0; targeted **65 passed**, full suite **290 passed**. Promoted the default schema target to v6. No restore was necessary and no scheduler action was taken. | Runtime-only live DB and milestone; tracked `gmv_core/migrations.py`, three migration/identity test files, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical item is `DB-003` domain check constraints and requires a new Project Owner authorization |
| 2026-07-19 | Project Owner-authorized first decision slice for `DB-003`, explicitly resolving its circular dependency with `DB-010` by defining the canonical Import Queue state machine needed for later implementation. Scope: analysis, architectural decision, dependency-map correction, and isolated prototypes/tests only; no migration, backup, or live database change. | Complete. Accepted `ADR_DB003_IMPORT_QUEUE_STATE_MACHINE.md`. Canonical future field `state` has exactly `new`, `processing`, `classified`, `approved`, `retryable_error`, `rejected`, `failed`, and `archived`; only eight listed transitions are legal; `rejected`, `failed`, and `archived` are terminal. `pending` is derived, not persisted. Error states require a non-empty error, all other states require null; confidence is nullable in [0,1]; approved/archived require a destination. Writer authority is split among enqueuer, worker, human reviewer, and custody writer behind one transactional Core transition API with canonical Event emission. Values present only in tests (`complete`, `reviewed`) are explicitly non-authoritative. Updated the Core-boundary ADR, `GMV_CORE_ARCHITECTURE.md`, backlog dependency edges, and Sprint 003/004 roadmap wording: DB-003 owns the decision contract, DB-010 owns later persistence/API enforcement, so no cycle remains. Added a pure, unused-by-production prototype and 25 isolated tests covering the exact initial state, graph, every forbidden edge, terminality, legacy-value rejection, errors, confidence, and destination rules. Full suite **315 passed**; Ruff/diff clean. Live remained v6, SHA-256 `131245ce...5735`, mtime `1784491957`, size 192512, enforcement 1, integrity OK, zero FK violations, zero queue rows. No migration, backup, live write, restore, or scheduler action. | `00_CONFIG/ADR_DB003_IMPORT_QUEUE_STATE_MACHINE.md`, amended Core-boundary ADR and architecture/dependency documents, `gmv_core/import_queue_state.py`, isolated queue tests, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical gate is the remaining non-queue DB-003 domain-policy decision before any migration 007 work |
| 2026-07-19 | Project Owner-authorized decision-only completion of the non-queue `DB-003` scope. Explicit decision: assign policy, normalization, and future checks for duplicated lifecycle statuses in `objects`, `resources`, and `plugin_metadata` to DB-013; keep DB-003 limited to authoritative OID grammar, Service Run outcomes, compatibility mode, confidence, and non-self Relations; keep Queue state implementation separate under DB-010. Migration, backup, and live changes excluded. | Complete. Accepted `ADR_DB003_AUTHORITATIVE_DOMAIN_SCOPE.md`. Lexical OID grammar is exact `AAA-000001` with `000000` forbidden, while supported-prefix/type consistency remains DB-008. Service Run outcomes are exactly `OK`/`ERROR`/`TIMEOUT`/`CANCELLED`; compatibility mode is integer 0/1; confidence is nullable and finite in [0,1]; Relation endpoints must differ. DB-003 adds no lifecycle-status check. DB-013 now explicitly owns stored-versus-derived authority, reconciliation, redundant field/view changes, and only then future status checks. DB-010 owns Queue confidence persistence during its atomic state rebuild. Backlog and Sprint 003 sequencing were updated accordingly; the Queue ADR cross-references the scalar-domain decision. Added a side-effect-free executable prototype and 32 new isolated tests; combined DB-003/Queue targeted tests **57 passed**, full suite **347 passed**, Ruff/diff clean. Live read-only evidence: v6, SHA-256 `131245ce...5735`, mtime `1784491957`, size 192512, enforcement 1, integrity OK, zero FK violations, and zero violations across all five domains. No migration, backup, live write, restore, or scheduler action. | `00_CONFIG/ADR_DB003_AUTHORITATIVE_DOMAIN_SCOPE.md`, updated Queue ADR/backlog/roadmap, `gmv_core/domain_constraints.py`, updated Queue prototype, isolated migration tests, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next canonical gate is isolated migration 007 implementation/tests for OID grammar, Service Run outcomes, compatibility mode, and non-self Relations; confidence remains DB-010-owned and live/default stay v6 |
| 2026-07-19 | Project Owner-authorized isolated `DB-003` migration 007 design/test slice for exactly four non-queue domains: lexical Object OID grammar, Service Run outcomes, compatibility mode, and non-self Relations. Required preflight, one atomic application, complete dependent-schema/data preservation, injected-error rollback, and positive/negative tests. Import Queue state, DB-010 confidence persistence, DB-013 lifecycle statuses, live DB, default promotion, backup, cutover, and restore excluded. | Complete. Added explicit opt-in migration 007 while keeping `CURRENT_SCHEMA_VERSION=6`. One transaction preflights all four existing-data domains, snapshots the two affected autoincrement sequences, rebuilds only `objects`, `service_runs`, `engines`, and `relations`, restores six dependent views and the v6 restrictive FKs, verifies `foreign_key_check`, and commits v7; Event triggers and all unrelated tables remain untouched. The runner now restores and verifies `foreign_keys=1` after script failure, and the static exception allow-list is exactly migrations 006/007. Tests cover accepted and rejected values for every constraint, four preflight failures before DDL, complete rows/views/triggers/indexes/sequences/FK preservation, idempotent explicit targeting, and exact rollback at two injected failure points (after table drop and immediately before version advancement). Deferred Object/Resource/Plugin/Engine lifecycle values plus deliberately out-of-range Queue confidence are preserved, proving non-enforcement of excluded domains. Migration/static **68 passed**; full suite **356 passed**; Ruff/diff clean. A temporary byte-copy of live v6 migrated to v7 with integrity OK, zero FK violations, and full table/view/trigger/sequence/FK parity. Live stayed byte-identical v6 at SHA-256 `131245ce...5735`; no backup, live write, restore, cutover, or scheduler action. | `gmv_core/migration_sql/007_domain_constraints.sql`, `gmv_core/migrations.py`, migration/static tests, amended DB-002/DB-003 ADRs and backlog, `00_CONFIG/PROJECT_STATUS.md` | this commit (see `git log`) | None approved — next gate is controlled preflight, verified milestone backup, and conditional live migration 007; default promotion requires successful post-cutover verification |

---

## 5. Document Index

*Canonical documents only, grouped by category. This is an index, not a copy — read the referenced file for full content.*

**ADR**
- `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`
- `00_CONFIG/ADR_CORE_PERSISTENCE_BOUNDARY.md`
- `00_CONFIG/ADR_MAIN011_CANONICAL_IDENTITY.md`
- `00_CONFIG/ADR_DB002_RESTRICTIVE_FOREIGN_KEYS.md`
- `00_CONFIG/ADR_DB003_IMPORT_QUEUE_STATE_MACHINE.md`
- `00_CONFIG/ADR_DB003_AUTHORITATIVE_DOMAIN_SCOPE.md`

**Freeze**
- `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`
- `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`
- `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`
- `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md`

**Suspension** (operational only — not a Freeze/legacy classification; final disposition pending)
- `00_CONFIG/FENIX_PROVISIONAL_SUSPENSION.md`
- `00_CONFIG/GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md`

**Specification**
- `00_CONFIG/SERVICE_SPECIFICATION.md`
- `00_CONFIG/LEGACY_ENGINE_INVENTORY.md`
- `00_CONFIG/OID_CONTRACT.md`

**Architecture**
- `00_CONFIG/GMV_CORE_ARCHITECTURE.md`
- `00_CONFIG/SOURCE_RUNTIME_BOUNDARIES.md`
- `00_CONFIG/RUNTIME_DATA_GIT_POLICY.md`
- `00_CONFIG/LOCAL_CODING_ENGINE.md` (experimental scope)
- `00_CONFIG/JSON_SQLITE_IDENTITY_MAPPING.md` (`MAIN-011` preliminary audit — evidence only, no migration decision)

**Status**
- `00_CONFIG/PROJECT_STATUS.md` (this document)
- `00_CONFIG/REBASE_001_CLOSED.md`
- `00_CONFIG/REBASE_001_INTERIM_STATE.md`
- `00_CONFIG/WHAT_TO_DO_NEXT.md`
- `00_CONFIG/SYSTEM_MAP.md` (permanent architectural map, not a status document — indexed here for discoverability)
- `00_CONFIG/STABILIZATION_EXIT_CRITERIA.md` (governance document — exit conditions for the Stabilization phase)
- `00_CONFIG/STABILIZATION_CLOSED.md` (closure record — Stabilization phase formally closed)

Additional pre-existing governance documents (security, retention, backup, versioning, and per-domain architecture references) exist under `00_CONFIG/` outside the scope of REBASE 001 / Post-REBASE 001 and are not indexed here.

---

## 6. Open Questions

*Intentionally unresolved. Not blockers unless stated in §1.*

1. Root cause of the persistent `gmv status` CLI failure observed under `launchd` execution (Post-REBASE 001 Tasks 4 and 6 — occurs even when the environment is correctly sourced, per Task 6).
2. Whether `gmv_snapshot.sh` has been silently failing since 2026-07-07 (confirmed: no new snapshot file created since that date; root cause not diagnosed — Post-REBASE 001 Task 6).
3. Classification of Dropbox `property_engine.py` (open since REBASE 001 Task 3; never resolved).
4. Correction: `~/GMV_CORE` was earlier described in this ledger as the "Local Coding Engine workspace root." This task (Stabilization Final Blocker Task) found `~/GMV_CORE`'s own `README.md` self-identifies it as "GMV RESEARCH LAB," an experimental prototyping lab — a distinct stated purpose from `00_CONFIG/LOCAL_CODING_ENGINE.md`'s "Local Coding Engine v0" concept. No evidence directly links the two; the earlier conflation in this ledger was not evidence-based and is corrected here. `~/GMV_CORE`'s root-level disposition is `PRESERVE_PENDING_REVIEW`; its two live LaunchAgents have a separate, resolved verdict (`PROVISIONALLY_SUSPEND` each) pending execution — see the Stabilization Final Blocker Task report.
5. Disposition of `gmv_watchdog.sh` and `gmv_orchestrator.sh` — flagged as sharing the same governance-contradiction evidence as the frozen GMV Engine and archaeologized Fenix, not yet individually archaeologized.
6. Fenix's final architectural disposition (keep/govern, merge, or decommission) — operationally suspended pending technical review (Post-REBASE 001 Task 7); see `FENIX_PROVISIONAL_SUSPENSION.md` for the minimum requirements that must be satisfied before this question can be resolved.
7. `com.gmv.dailybrief`'s and `com.gmv.morningbrief.email`'s final architectural disposition (keep/govern, merge, or decommission) — operationally suspended pending technical review (Stabilization Final Blocker Resolution); see `GMV_RESEARCH_LAB_AUTOMATIONS_SUSPENSION.md` for the minimum requirements that must be satisfied before this question can be resolved.
8. Whether `com.gmv.morningbrief.email`'s dormant external-email capability (to `gmv@area35artgallery.com`, via the same local SMTP relay used by `SRV-000002`) was ever actually exercised during its one manual test run on 2026-07-07 — unconfirmed either way; the capability remains preserved but inactive under suspension.
9. The origin and Dropbox-authenticity of `04_RESULTS/dropbox_decisions.json` (consumed by both `~/GMV_CORE` runners) was not verified — out of scope for the Stabilization Final Blocker Task.

---

## 7. Project Health

*Each an explicit qualitative status, evidenced by the referenced work.*

- **Architecture:** STABLE — core principles (Runtime belongs to Core; Dropbox is Repository; freeze before deletion; authority before execution; archaeology before implementation) established in REBASE 001 and consistently applied through Post-REBASE 001.
- **Governance:** IMPROVING — REBASE 001 resolved four long-standing ungoverned components; a previously-undiscovered automation family (`scripts/gmv_*.sh` plus `~/GMV_CORE`'s two LaunchAgents) was surfaced by the System Authority Audit; every live instance of it has now been suspended or frozen — no live, ungoverned, scheduled automation remains loaded.
- **Repository:** CLEAN — no unexplained tracked modifications as of the last audit (Post-REBASE 001 Task 3).
- **Runtime:** GOVERNED — one native service and three compatibility services are registered and documented; the previously-discovered ungoverned automation family is now fully frozen or suspended (none remains loaded/scheduled).
- **Compatibility:** STABLE — three compatibility services registered, hash-pinned, and documented; one output-rendering divergence formally resolved by ADR.
- **Legacy Review:** IN PROGRESS — four components frozen with documented reactivation conditions; `property_engine.py` remains unclassified; `~/GMV_CORE`'s root is now archaeologized (`PRESERVE_PENDING_REVIEW`) but its ~40 further scripts beyond the two audited (and now suspended) LaunchAgents remain unexamined.
- **Automation Review:** COMPLETE for all previously-live, scheduled automation — System Authority Audit found 12 untracked `scripts/gmv_*.sh` scripts across 6 LaunchAgents plus 2 further live automations in `~/GMV_CORE`; 2 scripts (`gmv_engine.sh`, `gmv_decision_engine.sh`) are frozen, 3 (`gmv_fenix_engine.sh`, `morning_os.py`, `dropbox_daily_brief.py`) are provisionally suspended; 3 remain unaudited but were already dormant/unloaded at last check (`gmv_watchdog.sh`, `gmv_orchestrator.sh`, and their further sub-scripts) — none of these three is currently live.
- **Development Readiness:** READY FOR CONTROLLED WORK — the evidence-first, report-then-freeze pattern is well-established, repeatable, and has produced consistent, reversible outcomes across every component examined to date; no blocking technical issue prevents continued work.

---

## 8. Next Session Bootstrap

**This is the mandatory session entry point.**

Future sessions must:

1. Read this document (`00_CONFIG/PROJECT_STATUS.md`) in full.
2. Read only the documents listed under **Required Context** below.
3. Continue from **Next Approved Task** (§1). If none is set, treat the
   highest-priority `PENDING_APPROVAL` item in §3 as the item requiring a
   Project Owner decision before work resumes — do not begin it unilaterally.

Do not reconstruct context from unrelated documents unless explicitly
instructed by the user.

**Required Context:**

- `00_CONFIG/REBASE_001_CLOSED.md`
- `00_CONFIG/WHAT_TO_DO_NEXT.md`
- `00_CONFIG/APPRENTICE_LEGACY_FREEZE.md`
- `00_CONFIG/CONSTITUTION_CLI_FEATURE_FREEZE.md`
- `00_CONFIG/REALESTATE_LEGACY_FREEZE.md`
- `00_CONFIG/ADR_MARKET_ENGINE_RENDERING_DIVERGENCE.md`
- `00_CONFIG/GMV_ENGINE_DECISION_AUTOMATION_FREEZE.md`
- `00_CONFIG/SERVICE_SPECIFICATION.md`
- `00_CONFIG/LEGACY_ENGINE_INVENTORY.md`
